**The Legend of Mir 2 浏览器个人游玩项目：可实施方案**

原始调研日期：2026-09-05。本文保留设计方案、验收范围和取证方法；当前实现状态、测试数字与运行模式以 [README.md](README.md)、[docs/implementation-status.md](docs/implementation-status.md) 和 [docs/delivery.md](docs/delivery.md) 为准。现行基线使用固定的 OpenMir2 子模块提交、`patches/openmir2/` 补丁链、WebSocket 网关和浏览器客户端；完整版本内容仍按后续阶段扩展。

建议以“国服 1.76、战法道三职业、经典像素素材与操作”为默认基准，采用 **TypeScript + PixiJS 浏览器客户端、OpenMir2 服务端、WebSocket 适配网关、离线素材转换工具**。该架构已作为当前实现主线；Crystal 保留为协议、帧表和素材解析参考。

另保留“Windows 原端 + 浏览器串流”的快速方案。如果最看重尽快个人游玩和保留原端表现，且接受一台图形主机持续运行，这条路线可以直接成为最终交付。两条路线各有完整验收标准，开工时只选一条作为交付主线。

**1. 先固定一个可验证的游戏版本。**

“老传奇”有国服、韩版、国际版及大量修改版本。复刻基准需要同时固定客户端资源、服务端规则、游戏数据、界面与操作习惯。仅凭某个压缩包标注“1.76”不足以确认版本。

| 项目 | 默认设计 |
|---|---|
| 玩法版本 | 国服 1.76 怀旧三职业；后续按明确的基准包逐项校准 |
| 使用方式 | 桌面浏览器、键盘鼠标、个人账号；支持本机与局域网 |
| 运行方式 | 浏览器负责画面与操作，本机或局域网机器运行游戏服务 |
| 首发浏览器 | Chrome / Edge；随后验证 Safari、Firefox |
| 画面 | 原始地图、人物、装备、怪物、UI、音乐和音效；800×600 经典逻辑画布为初始基准，实包确认后锁定 |
| 显示适配 | 优先整数倍缩放、等比留边、像素采样；高分辨率布局单独作为可选模式 |
| 角色进度 | 服务端存档，可备份和恢复；浏览器缓存仅保存可重新下载的素材和个人设置 |
| 数值模式 | 默认经典倍率；可额外提供独立的个人游玩配置档 |
| 完整范围 | 目标版本的地图、物品、怪物、技能、任务与NPC脚本、经济、成长、PK、行会、沙巴克规则 |

建立 version-profile.json，记录地图/技能/物品/怪物 ID 白名单、素材包 SHA-256、客户端及服务端提交、规则参数、参考录像和不确定项。素材与数值的依据按“同版本可运行客户端与数据、同版本源码、可重复实测、资料推定”分级。推定项保留标记，后续用证据替换。

完整版本的内容由清单定义。新手闭环只承担早期验收，后续阶段仍需覆盖清单中的全部目标内容。英雄、合击、内功等扩展系统不属于本次默认版本范围。

**2. 现有工程足以提供底座，但需要实测筛选。**

| 候选 | 本轮已核实内容 | 使用决策 |
|---|---|---|
| OpenMir2 | C# / .NET 8；README 指向 Delphi 1.76 客户端；保留登录、选角、游戏网关及数据库服务结构 | 中文经典路线首选，先做启动、规则与存档验证 |
| Crystal | 服务库及 Shared 已为 .NET 8；现有客户端与管理宿主仍有 Windows Forms / SlimDX 依赖；内容含额外职业 | 服务端后备；客户端逻辑、协议、帧表和素材解析的重要参考 |
| mir2x | C++23 / SDL3；项目当前说明以 v1.45 为参考 | 资源及实现参考；整体移植版本差距较大 |
| jootm2/client | PixiJS 7.4.3；已实现部分登录、地图与静态人物；PlayScene 当前仅处理新地图和登录等有限消息 | 抽取地图、素材、帧表参考，补齐客户端核心系统 |
| jootnet/JootM2 | 地图渲染框架，人物绘制仍有 TODO | 只复用有证据支持的资源与地图模块 |
| c-zhuo/Mir2 | README 明确说明游戏代码尚未发布 | 从可实施底座候选中排除 |

源码依据：[OpenMir2](https://github.com/mirbeta/OpenMir2/tree/1847483)、[Crystal 服务库](https://github.com/Suprcode/Crystal/blob/0e315fe327192afe52c3d7357ddd1f5b7e26c5b8/Server/Server.Library.csproj)、[Crystal 客户端](https://github.com/Suprcode/Crystal/blob/0e315fe327192afe52c3d7357ddd1f5b7e26c5b8/Client/Client.csproj)、[mir2x](https://github.com/etorth/mir2x/tree/873993d)、[jootm2 游戏场景](https://github.com/jootm2/client/blob/31ea8f00daafb1184ab9098dbfcc0ee2a4c76068/scene/PlayScene.mjs)、[jootnet 人物实现状态](https://github.com/jootnet/JootM2/blob/222dd0ef3716e2b0aa204f4c6e81a466eb819da0/src/actor/MapActor.js)、[c-zhuo README](https://github.com/c-zhuo/Mir2/blob/3cabac4fe0cf10979a28008a8f3313ec25994938/README.md)。

OpenMir2 的存储需要特别落实：账号、角色及游戏数据使用 MySQL，仓库提供三份 SQL；部分运行状态另存文件。本轮未发现必需 Redis 依赖。首版沿用原有持久化结构。配套数据中出现开天斩、逐日剑法及英雄技能，因此必须从技能、物品、掉落和 NPC 脚本的所有入口执行版本筛选。存档实现还发现属性字段绑定疑点，正式接入前必须核对数据库和重启结果。[MySQL 加载](https://github.com/mirbeta/OpenMir2/blob/1847483/src/DBSrv/AppServer.cs)、[游戏数据](https://github.com/mirbeta/OpenMir2/blob/1847483/sql/mir2_data.sql)、[存档实现](https://github.com/mirbeta/OpenMir2/blob/1847483/src/Storeages/DBSvr.Storage.MySQL/PlayDataStorage.Save.cs)。

**3. 浏览器主线采用四个清晰的职责层。**

| 层次 | 技术选择 | 负责内容 |
|---|---|---|
| Web 客户端 | TypeScript、Vite、固定版本 PixiJS 8、WebGL、Web Audio | 游戏画面、输入、动画、经典UI、声音、素材加载 |
| Web 网关 | C#、ASP.NET Core / .NET 10 LTS、WebSocket | 会话、旧协议编解码、登录/选角/入服切换、浏览器消息适配 |
| 游戏服务 | 固定提交 OpenMir2 与必要补丁 | 移动合法性、碰撞、AI、战斗、掉落、物品、交易、成长、世界规则 |
| 数据和资源 | MySQL、运行数据目录、静态素材包、内容清单 | 持久化、世界配置、资源版本、备份和恢复 |

游戏状态流为：浏览器输入 → WebSocket 网关 → 现有游戏网关与游戏服务 → 结算结果 → Web 客户端表现。素材由独立的 HTTP 静态资源路径加载。

网关复用服务端的中文编码、旧消息结构和会话逻辑，浏览器侧采用带类型定义的 UTF-8 JSON 消息，先降低调试成本。个人/少量客户端的消息量先实测，性能数据表明确有必要时再为高频消息增加二进制编码。网关不复制伤害、掉落或物品结算规则。

建议统一消息类别包括登录、角色列表、入图、对象增删、走跑、动作、属性、技能、物品、背包、商店、NPC对话、聊天、跨图和断线。每条消息定义会话/地图世代、顺序号及必要的实体 ID，客户端据此丢弃跨图后的旧消息。旧协议没有的信息由网关会话层补齐，并保持映射记录。

浏览器无法直接访问任意 TCP socket，需要服务端适配；即使改用 WebAssembly，这项约束仍然存在。网关必须正确处理 TCP 拆包/粘包与 WebSocket 消息边界，并管理有界队列、连接关闭和重连。[Emscripten 网络约束](https://emscripten.org/docs/porting/networking.html)、[WebSocket 与背压说明](https://developer.mozilla.org/en-US/docs/Web/API/WebSockets_API)。

登录后由网关完成传统登录网关、选角网关和游戏网关之间的连接切换。客户端只使用同源的一个 WebSocket 入口；网关连接的内部服务地址固定配置。重连后获取当前地图、角色和背包快照，清理本地待确认动作，避免自动重放购买、拾取、交易等有副作用的请求。

服务端沿用经验证的游戏时序。浏览器画面以 requestAnimationFrame 更新，源素材动作保留自己的帧时长。允许立即显示输入反馈与小范围移动预表现，受到拒绝时校正；HP、经验、金币和物品均以服务器结果为准。

**4. 素材转换是第一批核心交付物。**

需要准备一套尽量同版本的客户端 Data、Map、Wav/音乐目录，以及对应服务端的地图、怪物、物品、刷怪、掉落、商店、NPC脚本和技能数据。优先检查已有安装目录或备份；缺失时评估可访问的配套数据源，并将来源与哈希写入清单。现代客户端下载页不能证明能取得早期 1.76 素材。

OpenMir2 README 链接的 MirServer 包含地图、脚本及配置，也混有旧二进制；仅将已核验的数据与配置纳入素材候选，服务程序统一由固定源码重建。此阶段要产出“已取得/缺失/版本冲突”三类清单。[配套数据仓库](https://github.com/mirbeta/MirServer)。

素材处理顺序：

1. 原始数据只读入库：相对路径、大小、版本、来源、SHA-256。
2. 探测格式：WIL/WIX、WZL/WZX、具体 MAP 变体、音频格式；未知格式明确报告。
3. 解析像素：保留原图库名、原帧号、空帧、宽高、带符号 X/Y 偏移。
4. 建立动作映射：性别、衣服、武器、头发、方向、动作、帧序、持续时间和命中事件。
5. 转换地图：地面、细节、前景、阻挡、门、动画、光照、传送点引用。
6. 输出 Web 资源：初期无损 PNG 图集，JSON 元数据及二进制地图块；短音效与背景音乐分开管理。
7. 按依赖打包：公共 UI、角色动作、怪物组、技能组、地图块分别产包，内容哈希命名。
8. 生成素材审计报告、缩略图、帧联系图、缺失依赖报告和可重现构建记录。

Crystal 的解析器可参考多种 WIL/WZL 变体；其 LibraryEditor 为 Windows 工具，在 Mac 开发时抽取纯解析逻辑制作无界面 CLI。wil-viewer 适合人工查看，不能预设其解析器覆盖所有位深。jootm2 当前 WZL 分支仍待实现。[图库解析器](https://github.com/Suprcode/Crystal/blob/0e315fe327192afe52c3d7357ddd1f5b7e26c5b8/LibraryEditor/Graphics/WeMadeLibrary.cs)、[wil-viewer](https://github.com/XadillaX/wil-viewer)、[jootm2 格式选择](https://github.com/jootm2/client/blob/31ea8f00daafb1184ab9098dbfcc0ee2a4c76068/image/Images.mjs)。

帧偏移、透明色、阴影和混合方式必须保留。普通图像、火焰、雷电采用经参考画面对照确认的独立规则；统一将黑色删除或统一普通透明会产生错误。裁切图集后用元数据恢复原锚点，武器/身体/头发的前后关系按方向与动作确定。

地图使用解析所得逻辑网格。常见旧格式为 48×32 单元，存在多种文件布局；先识别再解析。绘制按地面、脚底位置、前景及特效层组织，单独处理树、屋顶、门、尸体和落地物。[地图读取](https://github.com/Suprcode/Crystal/blob/0e315fe327192afe52c3d7357ddd1f5b7e26c5b8/Client/MirObjects/MapCode.cs)、[动作帧表](https://github.com/Suprcode/Crystal/blob/0e315fe327192afe52c3d7357ddd1f5b7e26c5b8/Client/MirObjects/Frames.cs)、[渲染混合参考](https://github.com/Suprcode/Crystal/blob/0e315fe327192afe52c3d7357ddd1f5b7e26c5b8/Client/MirGraphics/DXManager.cs)。

原始包、网络压缩量、解码像素量和 GPU 常驻量分别统计。2048×2048 RGBA8 图集像素约 16 MiB，4096×4096 约 64 MiB，尚未包含临时副本及 mipmap。首版以 2048 图集页起步，按地图依赖加载，CPU/GPU 缓存分别管理。完整动作在战斗发生前加载，切图后按引用关系释放纹理。[PixiJS 纹理生命周期](https://pixijs.com/8.x/guides/components/textures)、[PixiJS 性能建议](https://pixijs.com/8.x/guides/concepts/performance-tips)。

**5. 还原工作按玩家实际体验拆解。**

| 系统 | 需要完成的行为 | 核对方法 |
|---|---|---|
| 移动 | 八方向、走跑、转向、停止、障碍、对象占位、追击、切图 | 固定输入回放，对照位置、每格耗时、转向和阻挡 |
| 人物 | 男女、装备组合、各方向全动作、死亡与复活 | 同坐标同帧截图，检查锚点、叠层和缺帧 |
| 近战 | 距离、攻速、前摇、命中事件、硬直、武器表现 | 参考录像加服务端事件时间线 |
| 法术 | 弹道、范围、持续效果、魔耗、施法限制、命中效果 | 固定目标/装备/技能等级下的结果与节拍 |
| 道士 | 符、毒、治疗、隐身、召唤物等级、跟随与攻击 | 按技能条目分别验证，涵盖切图与主人死亡 |
| 属性 | 攻魔道上下限、防御、命中、闪避、负重、持久、幸运等 | 使用固定输入与可控制随机种子的规则用例 |
| 怪物 | 仇恨、追击、攻击范围、刷新、Boss特殊机制 | 场景回放、规则日志和跨障碍测试 |
| 物品 | 地面图、背包图、装备图、拾取、堆叠、穿戴、丢弃、修理 | 操作前后数据库与客户端状态一致 |
| 经济 | 金币、买卖、仓库、交易、掉落归属与超时 | 断线/重复输入/服务重启后无丢失或重复 |
| 成长 | 三职业经验、升级、技能学习/熟练度、装备要求 | 经版本筛选后的数据表与升级流程 |
| 世界 | 城镇/洞穴连接、刷怪、商店、传送、任务/NPC脚本 | 地图与脚本引用清单全量检查，再走通路线 |
| 社交与竞争 | 聊天、组队、PK模式、红名、行会、沙巴克 | 至少两个受控客户端；攻城用可重复测试场景 |

画面还原包括经典血/蓝球、底部面板、背包格、装备窗、对话框、鼠标形态、小地图、文字位置、掉落名称与字体。游戏内界面主要用 PixiJS，中文输入采用 DOM 输入框处理 IME；输入状态暂停战斗热键。

浏览器对右键、功能键、失焦和音频启动有不同处理。游戏区域接管右键菜单，保留经典 F1–F8 映射，同时提供用户可改绑的备用键。系统占用的组合键列入兼容性记录。首次点击“进入游戏”时启用音频；回到标签页后重新同步服务端状态。[浏览器音频策略](https://developer.mozilla.org/en-US/docs/Web/Media/Guides/Autoplay)。

客户端不擅自增加影响经典行为的自动战斗、现代寻路和动作插值。每一项便利功能都作为独立配置，并在启用时保留清楚的设置说明。

**6. 先交付一个完整的小范围游玩闭环。**

首个可玩版本包含登录、三职业创建及基础操作、一个新手村与附近野外、一张初级洞穴、4–6种新手怪和一个测试Boss、约30–50件基础物品、药店/武器店/仓库，以及三职业各一个代表技能。

必须完整走通：进入游戏 → 移动 → 接近怪物 → 攻击/施法 → 击杀 → 掉落 → 拾取 → 穿装备/喝药 → 商店买卖 → 仓库存取 → 升级 → 死亡回城 → 退出 → 重启服务 → 继续进度。

地图、人物、怪物、装备、UI和声音使用目标素材；暂时缺失的资源写入缺口表，验收阶段逐项补齐。这个版本可供个人实际玩一段时间，并成为后续回归基线。

之后分区扩展：比奇/盟重及相关城镇 → 矿洞/骷髅洞/沃玛 → 蜈蚣洞/猪洞/祖玛 → 赤月/封魔/苍月相关内容 → 沙巴克和其余清单项目。具体地图、装备与Boss名称由锁定的版本清单决定，避免将不同补丁内容混在一起。

个人便利功能采用独立配置档：经验倍率、掉落倍率、刷怪等待、测试角色、GM传送、备份存档。默认经典档不受这些配置影响。机器人队友或攻城陪练放在完整基础规则之后评估。

**7. 实施阶段和投入估算。**

以下是工程估算，按一名熟悉 Web 游戏与 C# 的开发者全职推进、已有可用参考素材计算。代理可并行处理解析器、协议、客户端和核验，集成与实机验证仍需要连续投入。素材寻找、历史数值考证和 Windows 兼容问题会增加日历时间。

| 阶段 | 预计工作量 | 必须交付 | 通过条件 |
|---|---:|---|---|
| P0 底座与版本验证 | 3–5人日 | 版本清单、构建补丁、数据审计、运行记录、原端或协议测试端 | 能重复启动；由原端或协议测试端完成登录、服务端基础战斗、拾取、保存与重启恢复 |
| P1 素材与地图 | 5–8人日 | 无界面转换器、首区资源包、地图查看器、视觉对照 | 帧号/偏移/透明/阻挡正确，构建可重现 |
| P2 协议与进图 | 5–8人日 | Web网关、登录/选角/进图、对象同步、走跑与跨图 | 浏览器真实连接服务器，位置与碰撞一致 |
| P3 首个可玩闭环 | 10–15人日 | 战斗、掉落、背包、商店、仓库、死亡、声音 | 完整闭环与重启存档全部通过 |
| P4 三职业及核心系统 | 15–25人日 | 完整动作、技能/状态、换装、成长与经典UI | 目标职业技能清单和关键规则逐项通过 |
| P5 完整版本内容 | 20–35人日 | 目标地图、Boss、NPC、任务、装备/掉落、PK/行会/攻城 | 内容清单可追踪，规则与资源引用闭合 |
| P6 性能与交付 | 5–10人日 | 浏览器兼容、缓存、启动/备份脚本、使用文档 | 压力、重连、重启、恢复和安装验收通过 |

合计约63–106人日。预留20%–30%修复与版本校准余量后，可按 **约4–7个月** 规划较完整的 Web 版本。素材与底座顺利、P1与P2并行时，首个可玩闭环约 **4–6周**；单人全部串行可按5–8周规划。更准确的排期在P0结束时按实际缺口重算。

P0前3天用于淘汰不合适的底座，不能将其误读为从空目录3天完成网页传奇。若基础登录/战斗/重启存档仍无法跑通，应形成问题清单并验证后备路线，暂停扩展网页UI。若资源不完整，优先补齐首区所需材料，完整版本的时间保持为条件估算。

协议测试端可以证明服务端数据闭环，画面与动作还原仍需可运行原端或可信的同版本参考录像。两类验收证据分别记录。

P1、P2可以独立并行；P3依赖二者。P4完成后，地图内容可按区域并行导入，所有区域使用同一版本清单和同一规则服务。

**8. 验收以“能玩、像原端、存档可靠”为核心。**

以下指标是拟定目标，尚无本项目实测成绩。

| 类别 | 验收目标 |
|---|---|
| 可重复安装 | 干净运行目录按文档构建/导入/启动，进入同一参考场景 |
| 内容完整性 | 锁定版本清单中的目标项均有实现、素材和测试记录；缺失项为零 |
| 视觉 | 20–30个固定场景；静态非文字区域以像素叠图核对；所有八方向与关键换装无错位/切边/错误遮挡 |
| 操作时序 | 与基准相比，关键动作事件误差目标不超过一个源动画帧；逻辑位置和阻挡一致 |
| 规则 | 固定输入与确定性条件下的伤害/属性/消耗结果一致；随机掉落按概率表和统计样本核验 |
| 常规性能 | 在指定验收Mac/浏览器、800×600逻辑场景中以60FPS为目标，记录p95帧耗时和卡顿 |
| 压力场景 | 约100个可见活动对象、20个并发特效，记录p95/p99帧耗时、网络队列与纹理常驻量；以场景记录确定最终预算 |
| 内存 | 反复切图/换装/战斗30分钟后缓存可回落；初始GPU纹理预算256–512MiB，实测后调整 |
| 加载 | 代码/UI启动包目标10MB以内、首个可玩区资源目标30–60MB以内；基准资源审计后修订 |
| 存档 | 位置、等级、经验、金币、装备、背包、仓库、技能在正常重启后完全一致 |
| 网络异常 | 20次断开重连；2个客户端同时运行30分钟；无重复奖励、交易、掉落和异常状态残留 |
| 长时稳定 | 至少连续2小时游玩与自动场景回放；无增长失控、持续错误或存档损坏 |
| 恢复 | 将数据库与文件备份恢复到干净运行目录，实际登录核验状态 |

离散动画保留原始帧率，画面60FPS不改变游戏动作速度。加载体量和内存为预算目标，资源包实际清点后才给最终数值。

存档测试区分正常重启和进程异常退出。P0测明现有自动保存间隔和事务边界；在关键操作与退出时落实保存，并公开异常退出可能回退的最大窗口。遇到存储错误应明确报错，禁止把写入失败显示成成功。备份时协调数据与文件的一致性，个人模式优先采用保存、停服、备份、恢复启动的简单流程。

**9. 本机部署从现有 Mac 环境出发。**

浏览器客户端在 Mac 上开发运行。OpenMir2 首选在 Linux ARM64 容器中验证，MySQL和运行数据使用持久目录。需要处理硬编码盘符/输出路径、路径大小写、GB2312编码注册、动态 MySQL 存储插件发布和裁剪设置；ARM64依赖与二进制结构另做回归。直接在macOS运行时，部分Linux系统监控读取逻辑需适配。[平台实现](https://github.com/mirbeta/OpenMir2/blob/1847483/src/OpenMir2/Base/SystemEnvironment.cs)、[发布设置](https://github.com/mirbeta/OpenMir2/blob/1847483/src/GameSrv/GameSrv.csproj)。

底座在P0按原工程的.NET 8目标构建，建立行为回归基线后，再验证升级到.NET 10 LTS；新网关直接采用.NET 10。正式交付检查运行时支持期，将序列化、反射、插件加载和第三方依赖的兼容修复纳入预留工作量。Microsoft当前列出的.NET 8支持结束日期为2026-11-10，预计完整项目周期会跨过该日期。[.NET支持政策](https://dotnet.microsoft.com/en-us/platform/support/policy)。

资源规划初值：Mac开发建议16GB内存，预留20–40GB工作空间用于原始包、解码临时文件、图集与备份。独立个人服务器可先以2–4 vCPU、4–8GB内存作为试运行规格，实际容量由地图加载及AI负载测量决定。Web主线服务端无需渲染GPU。硬件数字为规划值，尚未做容量测试。

本地使用localhost；局域网部署统一HTTPS/WSS入口和账号访问控制，数据库与旧协议端口保留在内部网络。网页和资源包固定版本，存档放在持久目录。个人游玩初期可以完全在本机完成，无需购买云服务或域名。

浏览器素材使用HTTP缓存与必要的IndexedDB/Cache API元数据。缓存可能因配额或用户操作被清除，重新下载可恢复；角色进度始终在服务端。[浏览器存储说明](https://developer.mozilla.org/en-US/docs/Web/API/Storage_API/Storage_quotas_and_eviction_criteria)。

如果要求“关闭后台服务后仍完全离线，只运行一个网页”，还需要将游戏逻辑及持久化移植到浏览器运行时，这属于追加阶段。当前主线支持断开互联网后在本机后台服务运行的条件下继续游玩。

**10. 快速串流方案也可以单独交付。**

适用条件：接受Windows图形主机持续运行，且能够取得并启动一套配套客户端与服务端。链路为：

成熟原端与配套服务端 → Windows桌面运行游戏 → Sunshine捕获与编码 → Moonlight Web网关 → 浏览器接收视频/声音并回传输入。

候选使用Sunshine与社区项目moonlight-web-stream。该Web网关提供到浏览器的串流实现；具体版本固定到经测试的稳定发布，先在本机/局域网验证。[Sunshine](https://github.com/LizardByte/Sunshine)、[Moonlight Web v2.10.0](https://github.com/MrCreativ3001/moonlight-web-stream/tree/v2.10.0)。

| 步骤 | 交付内容 |
|---|---|
| 1 | 原端与服务端可启动，确认版本，完成打怪、掉落、存档 |
| 2 | Sunshine能稳定捕获窗口/桌面，视频与声音正常 |
| 3 | Web网关配置HTTPS和访问控制，浏览器能够启动会话 |
| 4 | 校准鼠标绝对坐标、右键、F1–F8、中文输入、音量与窗口缩放 |
| 5 | 加入一键启动、客户端断开后的行为、存档/备份和重连 |
| 6 | 连续游玩30–60分钟并录制输入延迟、文字清晰度、声音和切图结果 |

建议局域网输入到画面p95目标≤100ms，并检查编码导致的小字模糊与色彩变化。若主要热键受系统保留，提供映射入口并记录限制。客户端断开时明确选择继续在线或安全退出，避免角色意外留在危险区域。

已有可用Windows原端与图形主机时，串流接入通常可按1–3天验证、约2–7个工作日完成封装估算。该时间不包含寻找资源、修复服务端或解决虚拟机兼容。当前Apple Silicon Mac上的旧Windows客户端、DirectX、捕获和编码组合尚未实测，不能预设虚拟机或转译方案一定可用；优先复用已有Windows机器，若没有则另做环境验证。

这条路线能继承所选原端的画面与玩法，浏览器体验仍会受视频压缩和输入延迟影响。它可作为个人长期使用方式，也可只作为原端对照环境。确认串流已满足需求后，可以据此停止Web客户端重建。

**11. 后备路线和关键风险都有明确处理办法。**

| 触发条件 | 处理办法 |
|---|---|
| OpenMir2无法稳定运行或存档可靠性不足 | 固定问题样例，限时修复；随后验证Crystal服务库加无界面宿主 |
| Crystal作为后备 | 沿用其文件存档；裁剪额外职业，重新校准1.76内容和数值；客户端资产层保持独立 |
| jootm2协议无法对接 | 保留已验证的地图/资源参考，按选定服务端协议实现网关 |
| 素材与地图来自不同版本 | 建立图库ID映射及依赖检查，优先更换冲突资源；逐地图对照 |
| 原始数值无法证实 | 标记推定项，采用可运行基准或可重复实测，记录偏差 |
| 大图集导致卡顿 | 依赖分包、预上传、场景裁剪、纹理释放与上传预算 |
| 老客户端不能在本机兼容环境运行 | 将原端基准转移至可验证的Windows主机；网页开发继续在Mac |
| 历史资源完整性不足 | 先补首区闭环材料，完整内容工期按缺口更新 |
| 串流黑屏/输入/延迟不合格 | 改窗口捕获或环境做限时验证；未通过则取消近期串流交付承诺 |

Crystal切换需要按其协议编写新的适配器并复测规则、数据和存储。其服务库具备现代.NET目标，但这不能直接证明Linux部署已可用。[Crystal环境与存档](https://github.com/Suprcode/Crystal/blob/0e315fe327192afe52c3d7357ddd1f5b7e26c5b8/Server/MirEnvir/Envir.cs)、[协议定义](https://github.com/Suprcode/Crystal/blob/0e315fe327192afe52c3d7357ddd1f5b7e26c5b8/Shared/Packet.cs)。

WASM仅在有成熟可移植解析器、性能数据支持时引入局部模块。整个Delphi/DirectX或C#/Windows客户端无法靠一次编译直接成为可玩的Web项目；需要适配渲染、文件访问、主循环、输入、网络和声音。将整个旧客户端放入浏览器虚拟机也需要单独验证DirectX和运行效率，不计入主线工期。

**12. 最终交付应是一套能安装、能玩、能恢复的工程。**

目标目录结构如下；已实现的目录与脚本以工作树及 README.md 为准，其余在后续阶段生成。

~~~text
mir2/
  PLAN.md
  apps/web/                 浏览器游戏与经典UI
  services/web-gateway/      会话及旧协议适配
  vendor/openmir2/           固定提交的服务端源码
  packages/protocol/         Web消息与兼容测试样例
  tools/asset-pipeline/      素材探测、解析、转换、审计
  content/classic-176/       版本清单及世界/规则数据
  assets/raw/               原始素材，独立存放
  assets/web/               可重建Web素材包
  fixtures/reference/       原端截图、事件记录、规则样例
  tests/                    协议、规则、存档与浏览器回归
  deploy/                   Compose、入口服务与启动配置
  scripts/                  导入、启动、停止、备份和恢复
  docs/                     使用说明、版本差异、实测报告
~~~

交付包含完整源码与锁定依赖、素材导入命令、内容清单、数据库初始化/迁移、一键启动与停止、服务健康检查、GM与倍率设置、备份恢复工具、浏览器链接、已知限制、验收录像和测试报告。

最先执行的工作包是：固定1.76基准候选 → 验证OpenMir2和MySQL → 取得首区素材并导出 → 用原端或协议测试端证明战斗与存档 → 再接入浏览器地图、人物和操作。完成P0后，根据实测结果锁定底座、材料清单与后续排期。
