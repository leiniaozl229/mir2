diff --git a/src/GameSrv/Word/WorldServer.Switch.cs b/src/GameSrv/Word/WorldServer.Switch.cs
--- a/src/GameSrv/Word/WorldServer.Switch.cs
+++ b/src/GameSrv/Word/WorldServer.Switch.cs
@@ -125 +125 @@ namespace GameSrv.Word
-            for (int i = 0; i < playObject.SlaveList.Count; i++)
+            if (playObject.SlaveList != null)
@@ -127,2 +127 @@ namespace GameSrv.Word
-                IActor baseObject = playObject.SlaveList[i];
-                if (i <= 4)
+                for (int i = 0; i < playObject.SlaveList.Count; i++)
@@ -130,7 +129,11 @@ namespace GameSrv.Word
-                    //switchData.SlaveArr[i].SlaveName = baseObject.ChrName;
-                    //switchData.SlaveArr[i].KillCount = baseObject.KillMonCount;
-                    //switchData.SlaveArr[i].SalveLevel = baseObject.SlaveMakeLevel;
-                    //switchData.SlaveArr[i].SlaveExpLevel = baseObject.SlaveExpLevel;
-                    //switchData.SlaveArr[i].RoyaltySec = (baseObject.MasterRoyaltyTick - HUtil32.GetTickCount()) / 1000;
-                    //switchData.SlaveArr[i].nHP = baseObject.Abil.HP;
-                    //switchData.SlaveArr[i].nMP = baseObject.Abil.MP;
+                    IActor baseObject = playObject.SlaveList[i];
+                    if (i <= 4)
+                    {
+                        //switchData.SlaveArr[i].SlaveName = baseObject.ChrName;
+                        //switchData.SlaveArr[i].KillCount = baseObject.KillMonCount;
+                        //switchData.SlaveArr[i].SalveLevel = baseObject.SlaveMakeLevel;
+                        //switchData.SlaveArr[i].SlaveExpLevel = baseObject.SlaveExpLevel;
+                        //switchData.SlaveArr[i].RoyaltySec = (baseObject.MasterRoyaltyTick - HUtil32.GetTickCount()) / 1000;
+                        //switchData.SlaveArr[i].nHP = baseObject.Abil.HP;
+                        //switchData.SlaveArr[i].nMP = baseObject.Abil.MP;
+                    }
@@ -139 +142 @@ namespace GameSrv.Word
-            for (int i = 0; i < playObject.ExtraAbil.Length; i++)
+            for (int i = 0; i < playObject.ExtraAbil.Length && i < switchData.StatusValue.Length; i++)
diff --git a/src/GameSrv/Word/WorldServer.cs b/src/GameSrv/Word/WorldServer.cs
--- a/src/GameSrv/Word/WorldServer.cs
+++ b/src/GameSrv/Word/WorldServer.cs
@@ -318,0 +319,14 @@ namespace GameSrv.Word
+                        string invalidMapName = playObject.MapName;
+                        short fallbackX = 0;
+                        short fallbackY = 0;
+                        string fallbackMapName = GetHomeInfo(playObject.Job, ref fallbackX, ref fallbackY);
+                        if (SystemShare.MapMgr.FindMap(fallbackMapName) != null)
+                        {
+                            playObject.HomeMap = fallbackMapName;
+                            playObject.HomeX = fallbackX;
+                            playObject.HomeY = fallbackY;
+                            playObject.MapName = fallbackMapName;
+                            playObject.CurrX = GetRandHomeX(playObject);
+                            playObject.CurrY = GetRandHomeY(playObject);
+                            LogService.Warn($"角色 {playObject.ChrName} 的存档地图 {invalidMapName} 不存在，已回退到出生地图 {fallbackMapName}。");
+                        }
@@ -1403 +1417 @@ namespace GameSrv.Word
-                nX = SystemShare.Config.HomeY;
+                nY = SystemShare.Config.HomeY;
@@ -1804 +1818 @@ namespace GameSrv.Word
-                nX = SystemShare.Config.HomeY;
+                nY = SystemShare.Config.HomeY;
diff --git a/src/OpenMir2/Data/SwitchDataInfo.cs b/src/OpenMir2/Data/SwitchDataInfo.cs
--- a/src/OpenMir2/Data/SwitchDataInfo.cs
+++ b/src/OpenMir2/Data/SwitchDataInfo.cs
@@ -7,0 +8,12 @@ namespace OpenMir2.Data
+        public SwitchDataInfo()
+        {
+            BlockWhisperArr = new List<string>();
+            SlaveArr = new SlaveInfo[5];
+            for (int i = 0; i < SlaveArr.Length; i++)
+            {
+                SlaveArr[i] = new SlaveInfo();
+            }
+            StatusValue = new ushort[7];
+            StatusTimeOut = new int[7];
+        }
+
