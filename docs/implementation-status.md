# 实施状态

2026-09-08 更新。P0 基础闭环已有历史验证，经典主世界、洞穴线路和浏览器联机能力已纳入代码；完整范围仍以 PLAN.md 为准。最新整体 review 发现尚未关闭的交互和一致性问题，当前不视为完整交付验收通过。

## 2026-09-08 整体 review 与实机复核

完整问题、操作步骤、截图和证据等级见 [本轮 review](reviews/2026-09-08-review.md)。该次复核只更新了文档与证据；随后已按独立审计修复移动机制，其余 review 项仍按报告状态跟踪。

- 独立新账号完成法师创建、银杏入图、木剑 / 布衣穿戴、导师接近、古墓跨图、鸡的近战击杀、经验与一次升级，以及正常走 / 跑。
- 实机确认无交易时 Shift 点击蜡烛后持续禁用、操作结果隐藏、点击尸体只触发移动；国服聊天区只有内容输入，频道控件被隐藏。
- 代码验证补充动作确认关联、Shift 松键顺序、双首饰槽位与耐久下溢等问题。移动/攻击/施法的动作确认关联和 Shift 松键顺序现已修复；其余项目的优先级与复验条件见报告 R01–R10。
- `npm run build`、Python **79 / 79**、Core / Gateway / Castle 三套 .NET 回归通过。普通 / 100 对象静态帧预算各采样 8 秒，p95 均为 **9.1ms**；这项结果不覆盖联机战斗或长时稳定性。
- 当前运行 `MapInfo.txt` 只有 `0 / D001`，运行 `MapQuest.txt` 无条目；目录审计的 570 / 570 导出和任务审计 `ok=true` 均不能证明当前完整路线、Q001 任务已运行。
- 试玩期间出现 Colima 本机 Docker 连接 / 项目挂载不可用，引擎随后 exit 1，日志含 `Bus error`；页面自动重连结束后停在登录页。原因未定，未执行 VM 重启或存档恢复。挖肉入包、后续经济与存档恢复等本轮未完成项目已明确列出。

以下各节保留此前阶段的运行证据；涉及路线、任务、战争与停服恢复的结论应同时核对当时的运行配置和报告日期。

## 已取得的运行证据

- OpenMir2 六个服务在 Linux ARM64 容器运行，独立 MySQL 数据库正常。
- 协议探针已实际完成注册、登录、选服、角色创建、选角、公告确认和地图 0 入图，坐标为 289,616。
- 角色创建、战斗和移动后执行正常停服，重启后角色记录及最近一次确认状态均保留；引擎异常终止后的 60 秒自动保存窗口已用真实浏览器验收，`power_loss_probe.mjs` 又在 1,001ms 故障延迟内验证未保存移动回退，5 分钟网关稳定性窗口通过。真实主机断电和多小时运行仍待验收。
- Python 地图、协议、对象、物品、备份安全、路线地图、角色素材清单、内容审计、技能目录、任务目录、个人档和 P0 任务内容测试共 67 项通过。

新增 `tools/content_audit.py` 与 `tests/test_content_audit.py`。审计器从当前版本清单和源地图生成完整目录检查，逐项核对 570 张地图、Web 分块、地图依赖、路线向导和已锁定素材库；当前报告为 570/570 地图导出、依赖缺口 0、路线缺口 0、素材锁通过。报告写入 `.runtime/reports/content-audit.json` 和 `.runtime/reports/content-audit.md`，原始素材需要逐字节核验时使用 `--verify-hashes`。

新增 `tools/skill_catalog_audit.py` 与 `tests/test_skill_catalog.py`。`content/classic-176/skill-rules.json` 固定 15 个经典核心技能的效果号、魔耗、伤害/治疗参数、职业、三级门槛、修炼门槛和延迟；审计器逐字段对照运行 SQL。当前 108 条技能记录中目标清单 15/15、缺失 0、重复 ID 0、规则差异 0，报告写入 `.runtime/reports/skill-catalog-audit.json`。这项检查锁定了数值数据入口。视觉目录 15/15 已通过 `skill_visual_audit.py`；联机施法与召唤见下文 `skill-combat.json`。

新增 `scripts/apply-personal-profile.py` 与 `content/classic-176/personal-profile.example.json`。个人档默认保持经典倍率；显式传入 `--apply` 后可独立调整杀怪经验、怪物掉落概率、刷怪间隔，并为指定角色写入本机 GM 管理员条目。脚本默认只打印计划，支持临时运行目录回归，当前 Python 回归覆盖四项配置的写入与 GBK 掉落文件改写；浏览器网关和服务端运行中的角色仍需重启后读取新配置。

新增 `tools/movement_replay.mjs`，在真实 WebSocket 网关上从当前角色位置选择可通行且未被对象占用的方向，交替提交走/跑并逐步反向回到起点。当前运行实例完成 4 次走、4 次跑的服务端接受、0 次阻挡，序列号和地图世代均单调，最终坐标与起点一致；证据见 `.runtime/reports/movement-replay.json`。

浏览器断线处理已改为有限次数指数退避重连。账号凭据只保留在页面内存，连接关闭时清空移动、战斗、门重试、拾取和交互待决状态；重连成功后自动重新登录并选择原角色，超过五次才回到登录表单。前端 TypeScript/Vite 构建已通过。

## 本轮修复与验证

动态门链路已接入。旧服务端的门存在性判断已修正为“找不到门才返回”，网关支持相邻坐标的 `openDoor` 命令，投影 `SM_OPENDOOR_OK/SM_CLOSEDOOR`，地图层绘制服务端确认的门状态；移动被门阻挡时，浏览器会自动请求开门并在开门广播后重试一次。服务端现在同步切换同一门组的连续门格，并在关闭前向门格上的玩家发送关门广播。真实浏览器已从 `(307,627)` 连续穿过 `(307,626)`、`(307,625)`；C# 网关回归已覆盖坐标和开闭字段。

重连问题已取得连续运行验证。修复角色加载时会话 ID 被清零、保存确认后记录未移除、等待保存被错误当作加载失败，以及保存确认前提前标记 RcdSaved 的问题。此前的 socket 清理与空后台数组修复也已纳入当前构建。

协议探针连续两次完成入图和步行：291,616 → 292,616，下一次登录恢复到 292,616 后再走回 291,616。该结果证明本次场景的位置保存与重新登录恢复；后续章节另有物品、金币、仓库和单个技能的持久化证据。引擎配置 `SaveHumanRcdTime=60000` 后，真实浏览器把 WebCheck 从 294,610 移到 295,610，等待 65 秒再发送 `SIGKILL`；重启引擎并重新登录仍恢复到 295,610，证明异常退出的自动保存窗口已缩短到约 60 秒。

新增 `tools/web_register_probe.mjs`，通过浏览器同一条 WebSocket 连接实际执行 2002 注册、504 成功回包和随后登录，使用每次运行都新生成的账号，避免已有账号把注册回归变成假阳性。当前实测注册成功后立即收到空角色列表，证据写入 `.runtime/reports/web-registration.json`。新增 `tools/character_creation_probe.mjs`，使用全新账号继续回归空角色列表、战士创建、角色列表刷新和第一次入图，结果写入 `.runtime/reports/character-creation.json`。

新增长期任务脚本 `potion-quest.txt` 与 `weapon-quest.txt`，分别在比奇、银杏和边界出生区提供“药剂筹备”和“铁匠试炼”。任务沿用原生 `CHECK`、`SET`、`CHECKITEM`、`TAKE`、`GIVE`、`CHANGEXP` 和角色任务旗标，覆盖物品交付、经验/金币/药品奖励与放弃后重接；每个出生区的 NPC 都有独立可见的经典名称。`tests/test_p0_quests.py` 固定检查任务旗标、物品交付、奖励动作和三处 NPC 生成配置。

源数据包的 `MapQuest.txt` 已接入 classic-route 重建。当前源包提供一条可解析的 Q001 地图任务（骷髅精灵 5，NPC 脚本 Q002），运行目录会保留原始旗标、怪物、物品和脚本字段，并一并提供 `MapQuest_def/Q001.txt`–`Q010.txt`。`tools/quest_catalog_audit.py` 显式支持 `p0` 与 `classic-route`：P0 报告有意跳过的任务，经典路线校验必需任务并在缺失时失败。服务端已将 `MapQuest.txt` 注册到所属地图的击杀回调，击杀目标确认后会调用原生任务 NPC 脚本并交给 Q 脚本处理奖励与后续地图跳转，静态回归覆盖 PlayObject、地图解析器、任务管理器和 LocalDB 的接线。完整旧服任务仍需按版本清单继续筛选与逐条浏览器回归。

整组服务重启曾暴露停服竞态：Web 网关、游戏引擎和数据库通道并行退出时，最后一次首领战的 600 经验、678 金币、一瓶中量魔法药及最新坐标都回退到上一份存档。现在 `scripts/compose.sh stop` 固定按 Web 网关、在线角色数据库确认、游戏引擎、数据库的顺序关闭；`scripts/run-server.sh` 在引擎容器退出前也会请求保存。上游原有 `save` 命令此前没有启动控制台读取循环，现已接通，并最多等待 10 秒直到保存队列收到数据库确认。GameSrv 的停止路径改为可等待且幂等，并移除与 `WaitForShutdownAsync` 重复的宿主停止注册。重编译后的在线角色停服日志只有一次 Ctrl+C 和一次后台服务停止，未再出现集合遍历异常或未处理异常。补丁为 `patches/openmir2/0005-wait-for-player-save-acknowledgement.patch`。真实浏览器连续验证 `291,617`、`290,616`、`289,615`、`288,615` 四组停服前后坐标一致，最后一次同时恢复金币 73、经验 2485。证据见 `.runtime/reports/browser-online.json` 的 `normalShutdownPersistence`。

新增 C# 回归覆盖数据库未确认时保留待保存记录、成功确认后移除对应记录，并保留其他待保存记录；`tests/CastleRegression` 另外覆盖联盟文件写入、行会对象字符串污染、首次宣战登记、重复宣战续期、占领保护时间、敌方存活阻挡、皇宫换主和结束战役清场。两组 C# 回归均通过；Python 测试现为 67 项通过。

使用 `MIR2_PROTOCOL_TRACE=1 bash scripts/compose.sh up -d engine` 可启用阶段日志。日志记录包类型、长度、连接 socket 和加载/保存阶段，不输出账号口令或消息体。默认关闭，当前运行实例也保持关闭。

已实现对象位置、方向、名称、外观、死亡、经验消息解析，明确区分独立编码的角色描述与名称段。真实战斗已观察到鸡死亡并增加 5 点经验。挖肉发现无符号计数下溢（5 减少后变成 250），现已改为归零截断，并修复同类肉品质下溢。修复后挖肉新增“鸡肉”，已按原生 124 字节 ClientItem 解析物品实例 ID、名称、模式和持久。重新登录及服务重启后，位置与该物品的身份和持久均恢复一致。

较长运行暴露定时统计的零虚拟内存除零异常，以及空城堡防御单位保存空引用。两处均已修复并发布，C# 回归覆盖对应情况；城堡初始化还会跳过无坐标的空守卫槽位，启动日志不再产生假告警。尚未据此宣称长时稳定性验收完成。

城战 NPC 的边界路径已补齐：攻城申请菜单逐项显示城堡名称，非法城堡编号会返回可见提示，城门尚未生成时查询状态会返回 `unknown`，守卫与弓箭手雇佣编号同时校验下界、配置上限和实际数组长度。服务端重编译并重启后，城堡初始化无空守卫告警；单独运行 WebSocket 综合探针通过登录、属性、背包、移动、丢弃、地面状态和拾取闭环。

本轮稳定性回归定位到 `RegenMonsters` 的异常来源：动物刷怪诊断日志读取了尚未初始化的 `ItemList`，刷怪线程因此产生空引用。已移除这条调试读取，并让挖肉诊断采用空安全访问；重建引擎后观察约两分钟没有再次出现 `WorldEngine::RegenMonsters` 异常。临时召唤物的 `ActiveCount=1` 修复落地后，GameSrv 状态采样中对象数从正常刷怪后的 `18,451` 连续 10 秒保持不变，怪物数保持 `17,713`，未观察到每轮复制增长。脚本加载兼容层还为经典数据包补齐 `GAMEGIRD`、`GAMEDIAMOND`、`CHECKGAMEGIRD`、`CHECKGAMEDIAMOND`、定时器、攻击模式、滚动消息、英雄召回和网页提示等旧命令别名，并过滤管理脚本中的富文本说明行；启动日志不再出现此前那批未识别命令错误。长期多小时运行仍需继续观察。

加入 `scripts/wait-ready.py`，根据当前容器启动时间和后端连接日志等待启动，避免把旧日志当作新进程就绪。协议对象快照保存在 `.runtime/reports/world.json`。`--combat` 开启有限动作预算的战斗与挖肉探针。战斗证据独立保存在 `.runtime/reports/combat.json`，物品预期状态来自实际服务器消息。启动脚本按服务依赖顺序等待 Linux 监听端口，探测不创建旧协议网关连接。

地面丢弃/拾取已通过真实协议测试。恢复 MapItem 的非零 ID 分配，解决地图拒绝新建地面物品的问题；肉类落地扣减 2000 持久时改为归零截断，并只修改地面副本，保留被拒绝操作的背包原值。C# 回归覆盖真实地图注册、拒绝丢弃不改持久和低持久下溢。实例 4716833 的鸡肉由背包进入地面再拾回，身份保持一致，持久 1 → 0，地面对象随拾取消失。证据见 `.runtime/reports/drop-pickup.json`。

协议探针增加独立的物品基线与丢弃阶段证据，避免失败报告覆盖成功基线。修复前测试已造成鸡肉持久 2001 → 1，此损伤作为独立失败证据记录；没有修改数据库伪造恢复。`--drop-pickup` 可重复验证地面链路。

拾回后等待数据库确认并重启整组服务，探针恢复到 296,624，物品身份与零持久一致；再次丢弃/拾取仍保持零持久，未发生下溢。此结果覆盖该位置和单件物品；仓库及单个技能的正常重登证据见后续章节，完整角色与异常退出恢复仍待验收。

新增 `tools/power_loss_probe.mjs`，先正常保存探针角色建立基线，再移动到相邻格并保持会话连接，在 `SaveHumanRcdTime=60000` 窗口内强制终止引擎；脚本重启引擎和网关后重新登录，比较基线坐标与恢复坐标，并把故障延迟、未保存坐标和恢复结果写入 `.runtime/reports/power-loss.json`。当前实测在 1,001ms 内将角色从未保存的 `300,621` 恢复到基线 `300,622`，服务健康检查随后返回 ready。`tools/session_stability_probe.mjs` 又完成 5 分钟、100 次周期快照，序列号和地图世代无回退，主动重连后四组权威状态齐全。真实主机断电和多小时稳定性仍需继续验收。

## 地图素材与浏览器验证

取得 Crystal 补丁源的 ShandaMir2 Tiles、SmTiles、Objects 三个图库，共约 124 MB，记录固定文件长度与 SHA-256。`scripts/import-map-assets.py` 可校验下载并重建所需 PNG。`tools/crystal_lib.py` 解析 v2/v3 库，保留原帧号、带符号偏移、阴影与遮罩信息，并检查压缩像素长度。当前实际输入为 v2。

地图 0 的动画依赖补齐后导出 4,533 个非空帧，另有 102 个源库空帧，越界引用为零。空帧保留语义，未用替代图填充。约 41 MB PNG 按场景引用加载；图集与纹理缓存预算仍待实施。

经典主世界、洞穴线路和完整地图目录已按同一格式补齐。地图 1（沃玛森林）为 600×600、地图 2（毒蛇山谷）为 600×600、地图 3（盟重省）为 1000×800；570 张可解析地图的清单、分块哈希和源哈希均保存在 `assets/web/maps/*/map.json`，其中 121 张精选路线配置了分层向导与区域刷怪，其余地图配置了安全出生点和通用返城向导。`--refresh-classic-route` 还从源数据包筛选 3,000 余条刷怪定义，保留原地图坐标、范围和刷新周期，并为没有源刷怪记录的地图补充低密度安全刷怪。导入器合并所有地图的 Tiles、SmTiles、Objects 依赖，缺失帧保留为可审计的未解析引用，当前地图资源未产生缺失帧；地图 1、2 当前视口、地图 3 安全出生区和新增洞穴出生区均能正常渲染。

兽人古墓一层 D001 已按同一经典地图格式导出，尺寸 400×400，共 49 个分块；它依赖的 Tiles 913、SmTiles 167、Objects 4,318 个导出帧均无缺失。素材导入器会合并 `assets/web/maps/*/map.json` 的实际依赖，完整主世界合并后保留 Tiles 17 个、SmTiles 1 个源库缺失索引，并在清单中显式记录，当前视口未解析引用仍为零。地图查看器可在运行时按服务端地图 ID 载入对应清单并隔离分块缓存。

`apps/web` 已使用 TypeScript、Vite、PixiJS 8 建立地图查看器：800×600 逻辑画布、三层地图、八位动画计数、灯光加色混合及原偏移、坐标跳转、拖动与阻挡叠层。浏览器实查 296,624 和 330,266 场景，未解析引用均为零，灯光黑底问题已修正。地图切换、角色跟随镜头、动态门状态和阻挡开关统一经过串行渲染队列，避免 Pixi 显示树被并发提交破坏。此处证明候选素材可以渲染当前场景；原端逐像素对照、门的经典原始贴图、角色遮挡及战斗尚未验收。
联机人物渲染现在按服务端 `RaceImg:Appr` 二元外观字段选择库，只有缺少外观字段时才使用名称回退；同一个 Appr 在不同怪物族之间复用时不会串用素材。新增 `tools/monster_visual_audit.py`，对照锁定的怪物表和实际导出库逐项检查 P0 的 22 个名称，当前覆盖 22/22，其中骷髅精灵、毒蜘蛛、盔甲虫和僵尸2保留候选标记，未知怪物仍保留实体与名称并显示待补素材标记。

经典窗口的标签布局已覆盖角色、任务、技能、装备、背包、附近目标、地面物品、聊天、队伍、攻击模式、行会和玩家交易；商店、修理、仓库及 NPC 对话继续作为窗口上的模态层，移动、战斗和 HUD 保持可见。生产构建已通过，联机页可以从 HUD 顶栏和窗口标签打开这些面板。

`npm run build` 与 67 项 Python 测试通过。地图校验入口为 http://127.0.0.1:5173/ ，联机实施入口为 http://127.0.0.1:5173/play.html 。

## 角色与怪物素材

已取得并锁定 CArmour/00–13、CHair/00、01、CWeapon/01、02、03、04、06、19、NPC/00 与 Monster/000、003、004、006、019、020、022、024、025、026、030、032、045、046、047、061、062、070、072、103、112，共 35,738 个非空角色帧。十四套首区衣服库各含 1,616 个完整帧，男女使用同一动作表并按 808 帧切换；浏览器根据服务端 `GetFeature` 的衣服形状选择 CArmour00–13，未知更高形状回退基础衣服。临时把 `WebCheck` 的布衣切换为 `StdIndex=99` 的轻型盔甲后重启引擎，真实浏览器显示“轻型盔甲(男)”、防御从 0–2 变为 3–3，证明存档外形、能力和浏览器角色材质链路同时生效；验收结束后已恢复原布衣数据。基础男女角色按 Crystal Player 帧表及女性 808 帧偏移预览，身体与头发使用各自原始坐标偏移。六套 P0 武器库各含 832 个非空帧，男女武器偏移为 0 与 416；浏览器从服务端外观的武器字节选择素材，并按经典八方向规则在身体前后切换绘制层。真实男性战士 `War905` 的木剑在穿戴、卸下、再次穿戴和向右移动时随服务端外观与装备窗同步，缺失素材回退标记未触发。NPC00 的 42 个静态帧全部导出，浏览器直接以服务端外观高字选择帧；真实场景在 283,616 同时显示十名具名 NPC 的原始像素图，素材加载后圆形占位被移除。武器和 NPC 的原端逐帧校准仍待完成。新增 Monster/006、019、103、112 分别覆盖多钩猫、山洞蝙蝠、羊和虎蛇；Monster/020、022、024、025、026、030、032、045、046、047、061、062、070、072 继续按洞蛆、骨系、沃玛系、祖玛系、猪系和僵尸系接入对应服务端外观。新增库均通过动作表解析和资源导出，动作校验页可切换新增怪物并同步播放八方向站立、行走和攻击。

首批原始魔法特效已接入。哈希锁定的 Magic.Lib 按需导出 200 帧，Magic2.Lib 导出 8 帧，零空帧和零缺失。浏览器用服务端 17 施法消息启动施法者序列，用 638 的效果类型、效果号、目标坐标和对象号启动落点序列；火球术覆盖施法 0–9、16 方飞行与命中 170–179，治愈术覆盖施法 200–209 与落点 370–379，雷电术覆盖 Magic2 的蓄力 20–22 与落雷 10–14。真实法师对移动中的鸡施放火球时，橙色火球从角色飞到目标并展开命中爆焰；三级雷电按 7/9 消息在移动怪物所在位置显示全高雷柱。真实道士按 2/2 消息自施治愈时，绿色粒子从头顶展开、包围角色并消散，同时 HP 恢复到 40/40。换图或清场会销毁仍在播放的特效。

转换器补齐 v3 内置动作表解析，保留帧数、方向间隔、时长、反向和混合标记，增加动作表边界与双层遮罩回归。67 项 Python 测试和包含地图、动作、联机三页的生产构建通过。`/actors.html` 可切换候选素材并同步播放八方向；此页面为素材校验用途。

哈希校验后的地图与角色全量重复导入已通过，记录见 `.runtime/reports/asset-import.log`。

## WebSocket 网关

新增 `services/web-gateway`，使用固定镜像摘要的 .NET 10，完成旧编码、GBK、TCP 分帧和三阶段会话。Compose 发布回环地址 18800，默认限定浏览器 Origin。已有账号登录、角色列表、选角、公告确认、入图及步行已通过真实 WebSocket 探针：296,625 → 296,624 收到服务端接受状态。证据 `.runtime/reports/gateway.json`。

网关回归覆盖两个固定编码向量、1,024 种长度、非法编码、GBK、原生 124 字节物品、多记录背包及真实 socket 的分段/合并帧。账号注册、三职业角色创建、背包与地面物品、装备、药品、战斗、NPC、商店、仓库和部分社交事件已接入；行会创建已通过比奇国王 NPC 的真实浏览器链路回归，行会公告、封号配置、结盟与解除联盟已加入浏览器协议和界面。比奇试炼与猎人试炼均已通过真实浏览器完成接取、击杀/挖肉、交付和奖励领取，药剂筹备与铁匠试炼的原生脚本、任务旗标和三处 NPC 生成配置已由 P0 内容回归覆盖；全量旧服任务、攻城与其他事件仍待扩展。

## 浏览器真实入图与行走

`/play.html` 已连接同源 WebSocket 代理，支持已有账号登录、角色选择、地图世代清理、对象外观/名称/位置同步、移除、死亡和单步移动确认。地图查看器与联机页复用 `map-view.ts`，地图前景和角色按脚底深度排序。基础男女角色、两种发型和 P0 22 个经典怪物已有外观映射；完整装备、NPC、首领和其他怪物图像仍需补齐。新增 Monster/006、019、103、112 库已完成哈希锁定与导出，网关收到缺少素材的名称时会保留待补标记。

真实浏览器核验：独立 WebCheck 角色在 287,618 入图；姓名、身体和头发显示正常，周围可见真实鸡对象；点击右侧相邻格后收到服务器接受消息，UI 坐标和镜头更新到 288,618。服务更新前后均可登录。此前出现的 `%s` 姓名来自服务端旧 RankLevelName 模板，已修正为兼容替换，原生 C# 回归覆盖该问题。

网关对象投影回归覆盖独立编码的描述/名称、外观与位置、自身角色标识，并确认受击血量字段不会当作坐标。浏览器点击目标会按服务端确认逐格自动接近，抵达一格后执行一次攻击、对话或拾取；`movement_replay.mjs` 已按 600ms 走路和 400ms 跑步周期完成连续往返回放，经典窗口标签覆盖角色、任务、技能、装备、背包、目标、地面物品、聊天、队伍、攻击模式、行会和交易。

浏览器刷新后重新登录，实际恢复到移动后的 288,618，页面错误日志为空。联机页新增 `apps/web/src/classic-hud.ts`，在同一张 Pixi 地图上绘制经典风格的角色栏、HP/MP 球、经验条、坐标和 F1–F8 快捷技能栏；属性、资源、等级、经验、技能列表、技能修炼和移动确认均直接更新 HUD，快捷栏按钮与功能键共享选技入口。认证完成后登录和选角表单自动收起，经典画面置于状态栏之后，断线时恢复登录入口。小地图现已读取原客户端 `mmap.wil` 图像及 `MiniMap.txt` 的地图映射，支持小图、大图、关闭三态，显示角色与附近对象标记，并把地图点击接入主移动寻路与服务端动作确认。游戏画面内新增可切换的角色、任务、技能、装备、背包、附近目标、地面物品、聊天、队伍、攻击模式、行会和交易窗口，NPC 对话、商店、修理和仓库也通过同一模态层显示，原有服务端驱动控件会随标签切换复用。真实 WebCheck 角色进入后 HUD 显示战士、2 级、24/24 HP、18/18 MP、地图 0 的 288,610；生产构建通过。完整角色存档验收及原端逐像素窗口校准仍需补齐。

背包与地面物品已接入类型化消息。哈希锁定的 Items 与 DnItems 候选库分别导出 5,206 与 5,128 个非空帧。WebSocket 探针把实例 4716833 的鸡肉丢到地面、移动到落点并拾回，确认地面对象消失且物品身份和持久一致，证据见 `.runtime/reports/gateway.json`。真实浏览器显示了角色实战挖出的鸡肉（实例 8232416、持久 878/4000），并通过 UI 完成蜡烛丢弃、服务端确认移动和同格拾回。联机页现同时列出当前视野内的地面物品；点击远处条目会按权威移动连续接近，进入同格后自动拾取。WebCheck 实测蜡烛从 `(290,619)` 掉落后，角色从 `(289,621)` 自动回到 `(290,619)`，条目消失并恢复到背包；页面错误日志为空。

基础近战和挖肉已接入浏览器。网关从确认位置构造攻击消息，并要求挖肉目标在视野中、已死亡且相邻；浏览器显示附近目标、追近操作、连续攻击与受击帧、HP、死亡、经验和挖肉动作。点击活体目标后客户端会保持目标状态，按服务端节奏重复攻击；目标移动时重新接近，玩家手动移动或目标死亡时停止。真实 WebCheck 角色在 D001 对骷髅实测 HP `90 → 87 → 82 → 75`，同时接收服务端受击事件；此前还把 5/5 HP 的鸡逐次打到 0/5 HP，收到经验 +5，四次挖肉请求后获得实例 10291948 的鸡肉，页面显示持久约 0.2/4.0；浏览器错误日志为空。基础法术和装备影响见后续章节。

背包装备操作已接入旧服权威状态。网关解析 13 个旧服装备槽位，按物品模式验证穿戴位置，并从已确认的背包或装备记录构造穿戴、卸装和使用请求；浏览器只提交物品实例编号或槽位。真实 WebCheck 角色先后穿上木剑和布衣，装备窗与人物外观消息同步，重新登录后两个槽位仍从服务端快照恢复；随后使用小量金创药，服务端确认后药品从背包消失，再将木剑和布衣卸回背包。生产构建、C# 协议回归、既有 WebSocket 丢弃拾取探针均通过。药品回复过程和高级装备效果仍待专项验证。

角色能力包已按服务端 40 字节布局投影，浏览器面板显示等级、职业、金币、HP/MP、经验、攻防魔道以及背包、穿戴、腕力负重。固定协议向量和真实 WebSocket 探针均验证该投影；浏览器实测穿上木剑与布衣后，攻击由 1–1 更新到 3–6，防御由 0–0 更新到 0–2，魔防由 0–0 更新到 0–1，三类负重也同步变化。资源增量、升级、金币和负重更新已有类型化入口；受伤角色的药品回复消息已完成浏览器验证。

首次游玩入口已接入。注册请求在网关内构造两个独立编码的定长 Pascal 记录，账号只接受 4–10 位 ASCII 字母或数字；C# 回归校验记录分界和 GBK 字节长度。角色创建支持战士、法师、道士、男女和两种已导出发型，成功后重新查询服务端角色列表；网关在查询角色后等待旧选角服务的 1 秒防刷窗口，避免浏览器立即创建时请求被旧服务丢弃。真实浏览器从空账号 `wm0905x1` 注册成功，创建女性法师 `WMage905`，以 1 级进入地图 0 的 289,616，背包包含蜡烛、小量金创药、木剑和布衣(女)。角色初始 HP 15/16，使用药品后资源消息把面板更新到 16/16；再次登录显示角色 1 级，药品仍保持已消耗。浏览器错误和警告日志为空。

移动机制重构已完成，最终人工观感验收待执行。浏览器为移动、攻击、施法提交 `actionId + mapGeneration`，网关把旧协议确认投影为带 `kind` 的 `actionResult`；自机一次只预测一段，下一段同时等待对应 ACK 和 600ms 六帧动作完成，角色、分层步态与相机共用动作起点。键盘、右键、点击寻路和追踪统一进入意图调度；右键使用地图投影八方向、死区与滞回，寻路具有直行偏好和稳定转向规则。Crystal 素材恢复 0 上、2 右、4 下、6 左，当前装备八方向走跑预载，异步切换保留有效姿态；远端移动按确认事件排队。前端测试、生产构建、Python 网关契约 9 项、C# GatewayRegression 和离线架构审计均通过；真实 WebSocket 回放完成 22 次走跑、0 次拒绝并回到起点，相邻动作启动间隔 600–602ms。带登录角色的逐帧录屏、冷缓存、900ms 延迟注入和多人同屏长距离移动仍需执行，详见 [移动机制审计与修复](movement-architecture-review-2026-09-08.md)。

新增 Agent 实机试玩链路。`play.html?agent=1` 提供只读、定长的逻辑格/确认格/渲染格/相机/动作/输入意图观测，`tools/agent_playtest.mjs` 通过真实页面和键鼠执行移动场景，输出逐次 JSON、登录/入图/首帧/半程/结束截图与脱敏 WebSocket 摘要。首次完整运行通过 15 项检查：人物首帧可渲染、单步半程位移、早到 ACK 绑定、600ms 完成门控、人物与镜头锚点、长按三步节拍、直线点击、近水平右键两段跑步、返程复位和浏览器零错误；角色从 `(290,617)` 出发并返回该格，人物资源预载后约 `89.8ms` 可渲染。该运行还发现并修复国服创建角色控件在新资源库中误用旧 `2420–2434` 索引造成的启动异常，并为资源加载窗口补上可见占位。使用说明见 [Agent 实机试玩与调试](agent-playtesting.md)。

Agent 玩法回归扩展到职业图、装备、练级、地图、Boss 和掉落。一次性账号通过真实选角页确认男性战士帧 40、男性法师帧 80；背包加载轻型盔甲、乌木剑、青铜头盔、金项链、铁手镯和古铜戒指六类图标，穿戴后装备槽、`CArmour02`、`CWeapon01` 与纸娃娃均加载成功。法师通过 NPC 进入 D001，雷电术击杀鸡后收到 500 点经验并由 1 级升至 2 级，尸体挖出鸡肉；31 级复测用 14 次雷电击杀骷髅精灵，地面出现金币和中量魔法药，全部拾取后药水增加一瓶、金币由 0 增加到 273，浏览器错误为零。运行过程还修复了国服选角职业帧映射、换图后三秒内 NPC 对话丢失、目标追近遇阻停止、点击路径在非门障碍处悬挂及客户端施法距离超过服务端八格等问题。`tools/agent_gameplay_playtest.mjs` 默认在无头 Chrome 中运行并输出十一张阶段截图和完整报告，设置 `MIR2_AGENT_VISIBLE=1` 可观察过程。

个人加速档将杀怪经验设为 3 倍、怪物掉落设为 2 倍，并为比奇、银杏布置鸡、鹿、稻草人、多钩猫、钉耙猫、半兽人、森林雪人、虎蛇和半兽战士递进猎区。真实浏览器分别从两个出生区创建一级角色，确认初始经验 0/10、木剑和布衣可穿戴，普通近战击杀基础经验为 5 的鸡后收到 15 点权威经验并升到 2 级，随后可步行抵达稻草人猎区。完整流程共通过 37 项检查，随后完成装备、技能、古墓练级、骷髅精灵 Boss 和两件地面掉落拾取；`MIR2_AGENT_FRESH_ONLY=1 node tools/agent_gameplay_playtest.mjs` 可快速复测新角色路径。个人档脚本按当前已应用倍率计算差值，重复执行不会继续放大掉落表或改写刷新表，切换档位会清理上一档管理的刷新点。

NPC 对话与原始图像已接入。网关保存可见对象外观，只允许角色点击相邻的 NPC，并把后续 `@标签` 选择绑定到当前 NPC；服务端脚本中的文本、换行、装饰段和可选链接转换为结构化消息。浏览器从 NPC00 按服务端外观高字选择静态帧，并呈现对话文本和按钮。真实角色在 291,614 打开 292,613 的边界老兵脚本，选择“开区人民币奖励设置”后收到 `[冲级奖励]`、`[幸运奖励]`、`[攻沙奖励]`、`返回` 四个二级选项；在 283,616 的商店区同时显示十名具名 NPC 的原始像素图，素材加载后圆形占位被移除。浏览器控制台无错误或警告。

比奇试炼任务已接入同一 NPC 对话链路。`content/classic-176/p0/tutorial-quest.txt` 使用原生 `CHECK [1001]`、`SET [1001]`、`CHECKITEM 鸡肉 1`、`SET [1002]` 保存接受和完成状态；猎人试炼脚本使用 1003/1004 旗标，`deer-drops.txt` 与 `0015-seed-animal-harvest-meat.patch` 保证鹿尸可连续挖出标准“肉”。新增药剂筹备与铁匠试炼使用 1005–1008 旗标，分别交付小量金创药和木剑并发放原生物品/金币/经验奖励；`tests/test_p0_quests.py` 固定检查这两条脚本及其三处 NPC 配置。`patches/openmir2/0014-persist-quest-flags.patch` 让 MySQL 存档在 `characters_quest` 中以 128 字节 Base64 数组保存并在角色加载时恢复 `QuestUnitOpen`、`QuestFlag`、`QuestUnit`。`NpcProjection` 将脚本中的多条 `QMARK` 标记投影为 `quests` 数组，并保留兼容用的单个 `quest` 字段；浏览器任务栏按角色名写入本机缓存并显示可接取、进行中和已完成状态。真实浏览器已完成前两条任务的接受、交付、奖励领取与重登恢复，网关回归覆盖任务标记解析。

商店买卖现已接入旧服权威协议。目录、装备实例详情、购买、出售入口、询价和出售结果分别解析 645、652、650/651、646、647 和 648/649；网关将操作绑定到当前商人，并只接受目录、详情页或背包中已确认的对象。真实女性法师在边界杂货店读取到 8 种商品和 10 个蜡烛实例，零金币购买小量金创药收到“金币不足”；卸下木剑后服务端询价 25 金币并成功出售，再以 11 金币买入一个指定蜡烛实例。目录库存从 20 降到 19，角色金币按 0 → 25 → 14 更新，背包新增第二支蜡烛；重新登录后金币、两支蜡烛和木剑移除状态均保持。浏览器控制台无错误或警告。

商人修理已接入旧服权威协议。668 打开当前背包的修理清单，1024/671 完成实例绑定的修理询价，1023 与 669/670 完成修理及成败响应；成功时客户端同步服务端返回的金币、当前持久和最大持久。真实女性法师在边界武器店打开修理入口，完整背包清单由服务端返回，并对 8.0/8.0 的蜡烛询价得到 -1，界面显示“无需或无法修理”。真实战士连续战斗后卸下木剑，数据库确认同一实例 26006850 的持久为 3970/4000；0 金币普通修理后恢复为 4000/4000，浏览器同时显示“木剑 修理完成”和服务端“已经修好了”。服务端对话先于 669 到达的消息重排也已覆盖，网关会保留在途修理实例。更大磨损下普通修理降低最大持久的数值仍待专项验证。

装备持久变化现可实时投影。网关用 642 的槽位、当前持久和最大持久更新已确认的装备实例，持久归零时移除损坏装备；装备窗显示名称与持久，并保留精确值用于回归。真实战士穿木剑战斗时，浏览器内同一槽位从 4000/4000 即时更新到 3976/4000，全程没有卸装或重登。

P0 基础物品与分类商店清单已固定。`version-profile.json` 记录 36 件去重后的基础武器、服装、药品、戒指、手镯、项链和杂货，并在边界测试区复用配套服务端的六份分类商店脚本。首次浏览器读取发现新商人的库存为空：`LoadGoodRecord` 尚未实现，补货循环启动五分钟后才运行，并在补完第一类商品后提前返回。`Merchant.RefillGoods` 现支持启动强制补货、写回单项补货时间，并继续处理全部商品，补丁为 `0004-stock-all-merchant-goods-on-startup.patch`。重建服务后，浏览器立即读取到武器 7、服装 8、药品 4、戒指 4、手镯 4、项链 5 种商品，名称、价格、库存和装备成色入口均由服务端返回；连同先前验证的 8 种杂货，36 件清单全部可见。证据见 `browser-online.json` 的 `p0ItemCatalog`。

仓库协议现已接入。P0 世界在 286,609 增加一个复用经典麦家铺子脚本的边界仓库，`prepare-runtime.py --refresh-p0` 可确定性重建它。700、701–704 和 705–707 分别驱动存入入口、存入结果、仓库实例列表与取回结果；网关只使用当前背包和仓库内的已确认实例构造 1031/1032。真实浏览器将蜡烛实例 10592383 存入后，背包数量和负重从 2 降到 1；找回列表保留同一实例号和 8.0/8.0 持久，取回后恢复。再次存入并重新登录，仓库仍返回该实例，最终取回成功；浏览器控制台无错误或警告。

技能学习、单目标施法、自我施法、三种代表特效和被动训练现已接入。网关解析 84 字节 ClientMagic、独立编码技能列表、技能增删与熟练度更新，只允许使用当前已学习技能，并从已确认实体状态决定目标坐标和十二格距离。浏览器特效路由已覆盖版本清单中的火球、大火球、雷电、治愈、施毒、灵魂火符、抗拒火环、魔法盾、幽灵盾、神圣战甲、召唤骷髅、半月和烈火等施法前动作；缺少同版原始帧时沿用已锁定的 Magic/Magic2 帧表，服务端效果仍为权威结果。排查测试导师时发现脚本动作解析器把大多数命令编号减一，导致 `CHANGELEVEL`、`GIVE`、`ADDSKILL` 错派；修复后边界村和银杏村导师可提供同一套三职业技能书。真实女性法师由 1 级升到 7 级，领取并使用火球术秘籍，技能栏即时显示 0 级、4 MP、0/200；火球术把鸡从 5 HP 打到 1 HP，熟练度增到 7。第二次施法的即时资源快照为 52 → 50 MP，期间自然恢复 2 点，与 4 点魔耗一致。真实道士 `Tao905` 在 7 级学习 0 级治愈术；权威测试状态为 5/40 HP、26/26 MP，第二次自施收到“服务端接受”和 `2/2` 魔法效果，HP 21 → 27 → 29，MP 26 → 24 后自然回满，熟练度 0 → 3。真实战士 `War905` 在 7 级学习 0 级基本剑术，装备木剑后攻击为 3–6，近战击杀 5 HP 的鸡并得到 5 点经验，技能熟练度 0 → 5。三名角色重登后等级、技能与熟练度都由服务端恢复；道士重登为 31/40 HP，战士的 5 点经验和木剑装备也保持。浏览器控制台无错误或警告。

技能导师的职业测试入口已扩展为可回归的经典技能组：战士在 35 级学会基本剑术、攻杀、刺杀、半月和烈火；法师在 31 级学会火球、大火球、雷电、抗拒和魔法盾；道士在 22 级学会治愈、施毒、灵魂火符、幽灵盾和召唤骷髅，并领取护身符与灰色药粉。`content/classic-176/skill-combat.json` 固定 15 项用法（敌对/自我/开关/蓄力/被动），客户端 F1–F8 对自我和开关技能直接施放，网关允许省略 `targetId` 或传 0 表示自己；刺杀/半月/烈火的 `+LNG`/`+WID`/`+FIR` 投影为 `warriorSkill`。召唤物外观带 `kind:"slave"` 与服务端等级调色板，联机页用绿色描边标出。`tools/skill_combat_audit.py` 对照规则、客户端用法表和导师等级；`tools/skill_combat_probe.mjs` 用新账号走导师并逐项施法，证据写入 `.runtime/reports/skill-combat.json`，支持 `MIR2_SKILL_JOBS` 做单职业定向复测。导师菜单含 `@nearmonsters`（`MAPMOVE 0 292 623`）以便敌对技能就近打鸡。`RegenMonsterByName` 原先把召唤物登记到没有刷怪表的随机线程导致只扣魔不现身，补丁 `0020-summon-slave-without-mogen-thread.patch` 回退到已有怪物线程；一次性召唤登记时同时写入 `ActiveCount=1`，避免生成线程把它视作空缺槽位而重复补怪。最新三职业完整复测覆盖 15 项技能，战士、法师、道士均成功入图并完成施法，召唤物 `变异骷髅(Kbovc5lh)` 被识别为 `kind:"slave"`、等级色 `229`，随主人移动并对相邻鸡产生 `attacked:true` 事件；证据见 `.runtime/reports/skill-combat.json`。探针按施法前实体集合锁定新召唤物，并在确定性测试区等待敌对目标、走到相邻格后验证召唤物攻击，减少残留实体和刷怪时序造成的假失败。脚本使用 `CHECKJOB` 做职业门槛，客户端对 1/3、2/4、2/27、8/10 等已存在的服务端效果号复用锁定的 Magic/Magic2 帧。

客户端技能表现已覆盖版本清单中的主动技能 ID。`apps/web/src/magic-effects.ts` 现在为核心 15 个技能建立显式视觉目录：火球、治愈、大火球、施毒、抗拒、雷电、灵魂火符、幽灵盾、召唤骷髅和魔法盾绑定 Magic/Magic2 帧序，基本剑术、攻杀、刺杀、半月和烈火明确标记为被动，由服务端近战动作表现；额外旧服效果号仍保留独立映射。新增 `tools/skill_visual_audit.py`，对照 `skill-rules.json` 和素材库帧范围逐项校验，当前 15/15 通过。火球、治愈、雷电继续使用已校准的独立序列，其余主动技能显示对应代表序列，服务端效果仍为权威结果。施法回包正文按旧服实现为纯 ASCII 技能号，网关已按该格式解析，避免一位或两位技能号触发旧编码长度异常并中止游戏读取器。修改后真实法师 `WMage905` 再次对鹿施放火球术，收到服务端 `1/1`、浏览器显示 `魔法效果 1/1`，控制台错误和警告均为空。证据见 `.runtime/reports/browser-online.json` 的 `skillAnimationCoverage`。

经典状态字段已补齐到网关和 HUD。服务端 `SM_CHARSTATUSCHANGED` 的完整 32 位状态掩码现在投影为实体状态，`SM_MYSTATUS` 的饥饿等级单独投影；联机页在 HUD 顶栏显示绿毒、红毒、禁魔、蛛网、定身、隐身、神圣战甲、幽灵盾、魔法盾、石化、开天眼和饥饿提示，并保留实体的状态与命中速度字段供后续视觉滤镜使用。状态随换图和清场一起清理，生产前端构建通过。
实体表现同步使用这些状态：隐身角色降低透明度，石化角色转为灰阶，毒状态使用绿色色调；状态清除后恢复原始像素颜色。

角色死亡与回城复活已接入。浏览器收到自身 32/34 死亡事件后停止移动和交互，将角色面板更新为 0 HP，并显示回城按钮；按钮只在内存中复用本次登录凭据，关闭旧会话后自动登录并选择同一角色。验证过程中发现脚本引擎的 `KILL` 动作只设置死亡标记，保存时仍保留原 HP；两套脚本执行路径现都先把权威 HP 归零。真实女性法师在 285,608 死亡后自动回到安全区 287,616，以引擎规定的 14/30 HP 复活；7 级、14 金币、布衣、两支蜡烛、火球术与 7 点熟练度均保持，浏览器控制台无错误或警告。网关同时投影 27 为 `entityAlive`，C# 固定向量覆盖复活坐标与方向。

同一会话跨图已接入。引擎追踪确认 `MAPMOVE D001 152 362` 成功提交，而旧网关遗漏 633/634/800/801/806/807，导致服务端已换图、浏览器仍停留在地图 0；现已按换图语义投影这些消息。浏览器实测从比奇 285,608 进入 D001 的 152,362，显示相邻古墓向导和 386 个当前视口图块，未解析引用为零；随后同会话返回比奇 285,608，再次进入 D001。往返期间 7 级、14 金币、30/30 HP、65/65 MP、布衣、两支蜡烛、火球术与 7 点熟练度均保持，浏览器错误和警告日志为空。C# 固定向量覆盖传送后的自身对象与旧对象移除。

经典主世界传送与重连已接入。`scripts/prepare-runtime.py --refresh-classic-route` 写入地图 0/1/2/3 的 MapInfo、出生点、区域刷怪、四份世界向导脚本和安全落点；默认账号与存档保持不变。浏览器由比奇省的世界向导进入沃玛森林（240,300）、毒蛇山谷（510,474）和盟重省（332,328），每次同会话都收到地图清理、清单和自身对象；随后分别刷新页面并重新登录，角色仍能从服务端恢复到对应地图。三张地图的实时视口分别显示 390、565、354 个图块，未解析引用为零；盟重省已观察到盔甲虫与羊。客户端换图时清除旧地图实体与旧 NPC 对话，等待新地图首帧完成后才接受新对话，避免重连或 HMR 后展示上一地图的脚本文本。

经典精选路线已扩展到 121 张并接入同一传送机制：兽人古墓、天然洞穴、沃玛、矿区、祖玛、地牢、封魔谷、石墓、白日门、赤月峡谷、封魔谷城区、苍月岛和幻境均由原始 `.map` 导出，配置了原始地图碰撞、安全出生点、区域刷怪与独立向导脚本；导入器为全部 570 张可解析地图生成 1、4、16、25、49 或 64 个分块，未解析引用保持可审计。真实浏览器从比奇省的扩展路线向导进入 D501 `(200,200)` 并进入 D502，随后进入白日门 `(250,250)`，页面地图视口未解析引用为零；完整目录中的其余地图均已完成引擎加载、资源、碰撞和出生点校验，逐图战斗回归仍在进行。

经典路线的 NPC 数据也已按同一目录加载。`--refresh-classic-route` 会从源数据包筛选并规范化 127 条商人、3 条固定 NPC、49 条守卫定义，并导入全部 142 个源 `Market_Def` 脚本，覆盖 570 张支持地图中的有效地图标识，同时保留 P0 边界商店、任务 NPC 与分页世界向导；完整地图目录现在包含 1/2/3 三张手工校准地表地图，并通过单测校验 569 个非比奇传送目标。刷怪导入器会丢弃源包中超出地图尺寸的历史坐标，避免旧引擎刷新线程异常。经典路线同时导入源 `MapQuest.txt` 的 Q001 地图任务和 Q001–Q010 脚本，任务目录审计报告为 1/1 条、缺口 0，并确认 `任务NPC/老人-1.txt` 的 Q001 触发脚本已导入；击杀回调会将目标击杀交给所属地图的原生任务 NPC，`@@InPutString8` 输入也会写入脚本 `S8` 变量继续执行。干净路线重建已验证源脚本 142 个、运行目录缺失 0 个；引擎启动日志确认交易 NPC、管理 NPC、守卫、570 张地图和 37,054 条刷怪配置均完成初始化。

毒蛇山谷的源杂货店与测试世界向导曾同时位于 `(505,479)`，角色和名称完全重叠。世界向导现移至开阔可通行的 `(507,468)`，与杂货店、仓库、米氏分别相隔 11、8、12 格；现有浏览器窗口重连后确认两名 NPC 分开显示，附近目标面板也返回各自独立坐标。

安全区现由服务端统一保护。此前坐标判定只在整张地图标记为 `SAFE` 时才检查 `StartPoint.txt`，普通出生区实际未进入安全状态；同时完整地图的原始大范围 `MonGen` 可能覆盖出生点。现在普通出生点周围 `SafeZoneSize` 范围也会正确命中安全区，主动怪物首次刷新在范围内时会移动到最近的可行走安全区外格子，游荡时不能进入安全区，索敌和攻击也会在任一方进入安全区后停止；鸡、鹿等被动动物仍可保留在安全区。CoreRegression 覆盖大小写无关的安全点匹配、边界、主动种族分类和刷新落点迁移。
本轮又用同一账号从比奇向导进入 D002 `(200,200)`，页面显示 408 个当前视口图块、未解析引用为零、兽人古墓二层向导和骷髅实体；返回比奇后 HUD 与地图状态均恢复到地图 0，修复了异步换图时 HUD 短暂保留旧地图编号的竞态。

D001 已加入四类普通骨系怪物和一个首领的服务端权威刷新。刷新中心依次为骷髅 168,350、掷斧骷髅 176,350、骷髅战士 180,345、骷髅战将 185,345、骷髅精灵 190,340，入口 152,362 等待五秒只显示古墓向导。真实角色沿可通行路线走到 162,354 后，看见 166,355 的骷髅及原始动画；视口扩展到 468 个图块，未解析引用仍为零。骷髅随后追击并把角色从 30 HP 打到 0，死亡面板正常显示，证明刷新、实体投影、怪物外观、追击伤害和死亡处理在 D001 串成闭环。普通怪物刷新时间分别为 45、60、75、90 秒，骷髅精灵为 300 秒。

洞穴战斗与掉落已形成首个实战闭环。排查导师脚本时发现 `GotoLableQuestActionProcess` 在执行第一条已注册动作后直接返回，导致同一 `#ACT` 中的后续 `ADDSKILL` 与 `GIVE` 被跳过。引擎现会继续执行成功动作，并在动作失败时终止；修复已保存为 `patches/openmir2/0002-run-all-registered-script-actions.patch`。随后修复 `HUMANHP/HUMANMP` 修改非实时能力副本、缺少资源推送和旧 `%d` 消息格式的问题，补丁位于 `0003-fix-script-health-and-mana.patch`。浏览器实测一次导师选择把角色 HP 22/128、MP 241/541 即时更新到 128/128、541/541，同时完成 30 级、三级雷电术和中量药品准备。真实浏览器在 D001 用三次雷电术将 90 HP 骷髅击杀，经验从 0 增至 85；P0 确定性掉落生成金币和小量金创药，角色拾取后金币从 14 增至 73，小量金创药进入背包。该掉落覆盖只写入隔离运行目录，经典源配置保持原样。证据见 `.runtime/reports/browser-online.json` 的 `caveCombatDrop`。

首个洞穴首领已接入并完成真实浏览器击杀、经验和掉落拾取闭环。骷髅精灵由 D001 的 `(190,340)` 周围生成，服务端数据库给出 500 HP 和 600 经验，P0 刷新周期为 300 秒。为了重复验收，隔离的古墓测试入口先清除旧测试怪，再生成一只首领，把角色送至 `(198,332)`、补满 HP/MP 并临时开启无敌；首领测试官返回比奇时立即解除。女性法师先在 `(195,335)` 以三级雷电术取得魔法效果 7/9 和 `500 → 456` 的受击证据，随后完整击杀另一只 `500 HP` 首领，经验 `2485 → 3085`。地面出现中量魔法药与金币；背包预留两格后，药品数量 `21 → 22`，金币 `73 → 751`，两项记录均由服务端移除。金币配置值 500 会进入旧引擎的 `count / 2 + random(count)` 公式，本次实际掉落 678。外观 150 当前映射到已有 `Monster026` 候选素材，原端视觉校准仍未完成。证据见 `browser-online.json` 的 `caveBoss`。

无效用户命令现在只结束当前操作。此前在首领死亡后多发一次施法会让 `CastMagic` 抛出 `Magic target is unavailable`，外层异常处理随即关闭 WebSocket，使已经显示的地面物品无法拾取。网关现将可预期的命令校验异常转换为 `command_rejected` 并继续读取命令；浏览器收到错误时解除技能等待态。真实浏览器再次对死亡首领施法后仍显示两个可用技能按钮，并从 `(194,335)` 成功移动到 `(194,334)`。独立丢拾回归把中量魔法药数量 `23 → 22 → 23`。

一致性存档工具 `scripts/backup.py` 已实现创建、离线校验与恢复。创建前复用正常停服顺序并等待在线角色获得数据库保存确认，再导出 `mir2_account`、`mir2_db`、`mir2_data` 三个数据库，并收集行会和沙巴克文件；数据库口令不进入归档。恢复前拒绝绝对路径、上级目录、链接和设备节点，并校验数据库及每个状态文件的 SHA-256。真实演练生成 43,886 字节归档，恢复 297,820 字节数据库导出后自动启动服务；WebSocket 探针再次完成登录、属性、背包、移动和丢拾。修复停服顺序后又生成并校验 `normal-shutdown-verification.tar.gz`，数据库导出 305,105 字节，备份自动重启后浏览器仍恢复到 `288,615`、金币 73、经验 2485。早期恢复证据见 `.runtime/reports/backup-restore.json`，最新正常停服证据见 `browser-online.json`。

聊天已接入五个频道。浏览器通过 `{type:"say",channel,target,text}` 发送附近、喊话、私聊、组队或行会消息；网关校验非空、控制字符、私聊对象和 180 个 GBK 字节上限，再构造旧服 3030。服务端 40、101–104 投影成附近、组队、喊话、私聊和行会接收频道。聊天输入框获得焦点时既有移动快捷键不会触发。真实 WebCheck 发送“聊天频道回归”后收到 `WebCheck:聊天频道回归`；切换喊话频道后服务端返回等级限制提示，WebSocket 保持连接；私聊频道会显示并强制填写对象输入框。

基础组队已接入同一条旧协议链路。浏览器面板提供允许组队、创建、邀请和移除成员，网关将 `{type:"groupMode",enabled}`、`groupCreate`、`groupAdd`、`groupRemove` 映射到 1019–1022；659、660–667 分别投影开关、操作结果、解散和 `/` 分隔的成员列表。真实双浏览器以 `WebCheck` 和临时 `Group905` 在比奇 `(289,618)` 建立两人队伍，两个会话均显示 `WebCheck、Group905`，网关回归覆盖成功与失败结果；验证结束后已删除临时账号，基线角色回到 `0/289,618`、金币 0。证据见 `.runtime/reports/browser-online.json` 的 `group`。

玩家交易已接入同一条旧协议链路。联机页提供按角色名发起交易、放入/取回背包实例、设置金币、确认和取消；网关将这些动作映射到 1025–1030，并把 673、675–687 的打开、物品、金币、取消和成功消息投影到双方会话。服务端交易请求允许同地图相邻角色按名字建立会话，交易期间保留相邻范围校验，避免旧版必须正面相对造成浏览器无法发起的问题。历史双浏览器已验证金币和蜡烛交换；`tools/trade_probe.mjs` 当前又用 `.runtime/pvp-attacker.json` 与 `.runtime/pvp-victim.json` 两个独立 WebSocket 会话完成同图相邻交易，确认双方 `tradeOpened`、物品放入、`tradeSuccess`，并从接收方 `itemAdded` 校验实例号和名称，发送方的交易放入结果同时确认移出网关背包。报告见 `.runtime/reports/trade.json`。

攻击模式已接入旧协议链路。联机页的七档选择框发送 `{type:"attackMode",mode}`，网关映射为 1046，服务端回传 213 并由浏览器读取 `Recog` 字段更新状态。真实浏览器以 `WebCheck` 验证“全体攻击 → 红名攻击 → 全体攻击”，页面、服务端状态和系统消息同步更新，证据见 `.runtime/reports/browser-online.json` 的 `attackMode`。夫妻、师徒、编组和行会模式的过滤规则仍依赖对应关系与行会数据，后续会单独回归。
行会面板已接入旧协议链路。浏览器提供打开行会、查看成员、在国王 NPC 对话中创建行会、邀请和移除、公告、封号配置、结盟和解除联盟、向目标行会宣战和打开攻城申请入口；附近其他玩家会出现在目标列表，点击后可按距离接近并提交近战，攻击模式、和平规则和行会关系仍由服务端裁决。网关把 `guildOpen`、`guildHome`、`guildMembers`、`guildAdd`、`guildRemove`、`guildNotice`、`guildRanks`、`guildAlly`、`guildBreakAlly` 映射到 1035–1045，并把 `guildCreate`、`guildWarRequest`、`castleWarDialogue` 转成当前国王 NPC 的 1011 对话命令；宣战请求会先进入国王的战争二级对话，再提交受保护的 `@@guildwar` 动作。750、753、754、756、757–763、768–771 投影为行会名、行会信息、公告/战争/关系、封号分组、无行会结果和创建/成员/联盟操作结果；战争关系支持 `warGuildTimers` 的剩余毫秒，浏览器按秒刷新倒计时，服务端输出行会名与剩余时间；创建费用与沃玛号角、掌门权限、宣战金币、目标行会、攻城头像和城堡日期由服务端校验和持久化。服务端现在会写入联盟行会名并在重启读取联盟列表，首次宣战会创建双向战争记录；`tests/CastleRegression` 已覆盖联盟持久化、首次宣战注册、重复宣战续期、占领保护时间、敌方存活阻挡、皇宫换主和结束战役清场。攻城 NPC 菜单逐项显示城堡名称，非法城堡编号、空城门状态和守卫雇佣下标已做服务端边界校验。空行会运行库的 `WebCheck` 已实测收到 754；补充 1,000,000 金币与沃玛号角后，通过比奇国王 NPC 创建 `WebGuild`，收到 762 并读取成员 `WebCheck`，服务端扣除创建费用并移除号角。服务端为行会目录创建缺失的 Linux 子目录，避免旧配置路径导致创建请求悬挂。`tools/guild_war_probe.mjs` 配合 `scripts/run-guild-war-known-fixture.sh` 已用两个独立 WebSocket 会话完成临时角色创建行会、国王 NPC 宣战、双方战争关系和倒计时递减验证，证据写入 `.runtime/reports/guild-war.json`；夹具结束后会精确清理角色、行会文件并恢复服务。前端构建、网关探针和 67 项 Python 回归通过；`tools/pvp_probe.mjs` 以两个独立 WebSocket 会话完成同图玩家发现、攻击模式设置、相邻近战和双方 `health` 事件回传，证据见 `.runtime/reports/pvp.json`。完整沙巴克联机场景、攻城计时和联盟战斗仍依赖专项场景回归。

首批声音已接入。`asset-sources.json` 固定 Crystal Sound 目录中的 `1.wav`、`50.wav`、`60.wav`、`022-1.wav` 和 `levelup.wav`，共 930,940 字节，全部是 44.1 kHz、16 位 PCM WAV；导入器校验大小和 SHA-256 后复制到浏览器资源。客户端把它们分别绑定到移动、挥击、受击、骷髅攻击与升级事件，并提供持久化声音开关。浏览器实查声音开关由开切换到关再恢复为开，随后服务端确认角色从 289,618 走到 290,618；自动化环境无法代替人工听感与音量平衡验收。

P4 经典界面已建立 2003 国服基准契约。`content/classic-176/national-ui-profile.json` 固定 800×600 画布、WIL/WIX、WZL/WZX、PAK 三种输入形态、NewopUI/Prguse/Prguse2/Prguse3/ui1/ui3/stateitem/ChrSel/mmap/MagIcon/Items/DnItems 素材族，以及 F9/F10/F11 经典窗口快捷键。`tools/validate-national-ui.py --data-dir <Data>` 会在解码前检查本地客户端素材是否成套存在；`tools/import-national-ui.py --data-dir <Data>` 已支持经典 WIL/WIX 与压缩 WZL/WZX 的隔离导出，保留帧号、锚点和透明调色板。已用本地 2003 客户端安装包完成只读解包，成功导出 Prguse、Prguse2、ChrSel、mmap、stateitem、MagIcon、Items、DnItems 八组核心素材，共 2,756 帧；联机页检测到 `assets/web/ui-national` 后会优先使用登录、选角、主 HUD、技能、背包/装备、地面掉落和角色/NPC 窗口的原始图像帧。NewopUI、Prguse3、ui1、ui3 在该客户端包内缺失，已按版本契约标记为可选族。Crystal 素材继续作为缺少本地导入产物或对应单库缺失时的回退候选，原端逐像素位置、文字基线和交互热区仍需逐窗口校准。

`tools/world_catalog_audit.py` 从锁定地图与源 `MonGen.txt` 生成可追踪刷怪清单，对照 `monsters` 表、MonItems 掉落文件和已导出外观；P0 22 怪基线必须命中，缺 SQL 会使内容审计失败，缺掉落/缺外观记入报告但不单独否定 570 张地图闭合。`tools/frame_budget_probe.mjs` 与 `apps/web/perf.html` 已完成 800×600 比奇参考场景采样：基准和 100 对象压力场景 p95 均为 `16.7ms`，约 `59.88 FPS`。`tools/reconnect_probe.mjs` 已以同一角色连续 20 次重连并收到 `attributes`、`equipment`、`inventory`、`skills` 四组权威快照，最终停留在地图 0 的 `286,620`。Safari / Firefox、连续 2 小时稳定性与沙巴克联机战役仍待验收。

## 后续完整交付

攻城浏览器专项探针已补齐：`tools/castle_war_probe.mjs` 从比奇扩展向导进入 0122 皇宫，打开比奇国王的攻城入口和资格校验，默认停在不会消耗道具的路径，报告写入 `.runtime/reports/castle-war.json`。当前实机结果为通过：扩展向导分页、皇宫传送、国王 NPC、`@requestcastlewarA` 和 `@requestcastlewar` 均收到预期回包，动态占位拒绝后会自动绕路。经典脚本实测顺序为先缴纳金条，再检查祖玛头像并展示城堡列表；准备金条后设置 `MIR2_CASTLE_EXPECT_LIST=1`，会继续检查城堡列表入口，非法城堡编号由 `scripts/run-castle-regression.sh` 覆盖。双行会夹具的可选提交模式已完成有效城堡申请、许可回包、祖玛头像扣除和沙巴克申请文件持久化验证；完整攻城计时、沙巴克占领和联盟战斗仍保留为专项实机缺口。

仍需完成素材版本校准、Web 网关的其余消息与会话功能、完整怪物与任务内容、原端逐像素窗口校准、真实主机断电和多小时稳定性验收。联机测试入口已有，主世界多地图、P0 战斗闭环、两条可持久化任务、扩展后的经典窗口标签、ChrSel 登录选角和窗口内引擎崩溃回退均可重复运行。

本轮补齐城堡初始化生命周期：待地图与行会加载完成后再初始化城堡，按 `Castle/List.txt` 去重读取配置目录，并保留真实目录名写回列表；100 系统广播会投影 `castleWar` 状态，行会面板显示攻城开始、结束提醒和占领结果。`tests/CastleRegression` 已覆盖城堡目录去重，网关回归覆盖对应的沙巴克系统消息字段。

本轮继续推进国服 UI 复刻：新增 `national-ui-profile.json` 和 `validate-national-ui.py`，把 2003 原始 1.76 国服 Data 作为视觉基准，将 Crystal 素材降级为候选回退；补齐 F9/F10/F11 窗口快捷键。`/ui-calibration.html` 固定 800×600 设计坐标，提供登录、选角、创建角色、主 HUD、角色窗、背包窗和 NPC 对话场景；支持载入国服截图做内存透明叠加、16px 网格、坐标尺和鼠标设计坐标读数。联机页检测到导入产物后，登录和选角使用 `ChrSel#22`、`Prguse#60`、`Prguse#65`，主 HUD 使用 `Prguse#1`，角色、背包和 NPC 窗口使用 `Prguse#370`、`Prguse#3`、`Prguse#402`。联机页接入同一套 `ClassicStage` 缩放器，窄窗口只缩放完整游戏画面，不重排国服控件。校准台、联机页和素材契约通过生产构建与 Python 回归。

本轮继续补齐动态窗口：商店、修理和仓库在国服素材可用时复用 `Prguse#402` 原始窗口框，按钮热区仍由现有服务端交互控制；商店成色详情、出售列表、修理列表和仓库实例会异步加载 `Items` 国服图标，并在素材到达后刷新当前窗口。`stateitem` 已接入角色装备页，安装包中的同编号帧与物品 `Looks` 对应；30–79、80–99 等区段的大尺寸武器、衣服和头盔使用素材锚点叠入角色区，首饰和蜡烛使用 `Prguse#378` 的实测槽位。背包保持图标原始比例，技能页按 `magicId` 显示 `magic-icons`，分页切换会彻底隐藏装备层。Agent 试玩会同时检查这些图标的加载、比例、锚点和分页隔离。

尸体挖肉已改为 `Alt + 左键` 专用交互。普通左键点击尸体只显示操作提示，不会自动接近或发送 `butch`；组合键会携带挖肉意图完成自动接近，遇到临时阻挡重新寻路时也会保留该意图。附近目标列表同步标注“Alt+左键挖肉”，Agent 试玩先验证普通点击不产出肉，再使用组合键完成采集闭环。

背包交互已按 2003 国服视觉与 Crystal 行为参考重新校准。`Prguse#3` 的可见网格改为 `(18,14)` 起点、`36×32` 步进，修复此前误用 Crystal `Title#196` 的 `(9,37)`、`37×33` 参数造成的逐格漂移。物品支持悬停属性、左键拿取后移动或交换、双击使用/穿戴和右键快捷使用；角色金币会同步写入背包底栏。网关从原生 124 字节物品记录补充投影攻防魔道、等级要求、价格及附加属性。角色、背包、通用、NPC、商店、修理和仓库窗口现可在 800×600 区域内拖动并保存位置。详细取证与后续复刻流程见 `docs/client-ui-replication.md`。
