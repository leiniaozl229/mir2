# 浏览器传奇复刻工程

实施范围与验收要求见 [PLAN.md](PLAN.md)，阶段记录见 [docs/implementation-status.md](docs/implementation-status.md)。P0 基础闭环、经典主世界与 570 张可解析地图目录（其中 121 张精选路线配置了分层向导和区域刷怪）、浏览器网关和技能导师已可重复运行；完整 1.76 内容、全量素材校准和经典 UI 仍在扩展。动态门协议已接入，阻挡移动收到服务端拒绝后会尝试开门，门的开闭状态实时投影到地图。

联机测试入口：http://127.0.0.1:5173/play.html 。当前已能注册账号、创建战士/法师/道士、进入真实比奇、沃玛森林、毒蛇山谷、盟重省、兽人古墓、沃玛寺庙入口/一层、废矿入口/矿区通道和石墓入口/深处，显示基础角色、鸡鹿、区域怪物和四类骨系怪物，完成点击或键盘走跑、跨图、跨图重连、基础近战、击杀经验、尸体挖肉、死亡回城、五频道聊天、两人组队、玩家交易、攻击模式、行会面板、基础事件声音、NPC 对话、比奇/猎人/药剂/铁匠四条早期任务、商店买卖、仓库存取、背包、穿脱装备、使用药品、物品丢弃、地面显示与自动拾取，以及技能书学习、目标施法、自我施法、魔耗、治疗、伤害和熟练度更新。技能导师按职业提供攻杀/刺杀/半月/烈火、大火球/雷电/抗拒/魔法盾、施毒/灵魂火符/幽灵盾/召唤骷髅测试套，服务端技能状态与对应代表特效会在重连后恢复。洞穴 P0 还提供三级雷电术和测试药品，真实浏览器已击杀骷髅并拾取金币与小量金创药。行会面板现在覆盖打开、成员分组、公告、封号配置、结盟和解除联盟，并提供宣战和攻城申请入口，均由旧协议服务端校验。联机页已加入覆盖在地图上的经典 HUD：HP/MP 球、经验条、地图坐标、职业/等级和 F1–F8 技能快捷栏与键盘选择同步；网关短暂断开时页面会自动有限重连并恢复原角色。需启动服务端、WebSocket 网关和 `npm run dev`；本机独立 UI 测试账号记录在 `.runtime/web-ui-test.json`。技能数值、召唤物行为、全量任务、完整战争计时与占城规则、完整经典 UI 仍在专项接入；比奇试炼和猎人试炼已通过真实浏览器完成交付、奖励领取与重登状态恢复，药剂筹备和铁匠试炼已加入三处出生区并由内容回归覆盖，行会创建已通过比奇国王 NPC 的真实浏览器回归。

## 浏览器地图校验

已提供 PixiJS 地图查看器与联机页面：地图查看器支持拖动、坐标跳转、阻挡叠层及地图灯光动画，联机页面通过 WebSocket 接入角色、对象和战斗状态。先按下文准备运行数据与地图导出，再执行：

```sh
python3 scripts/import-map-assets.py
npm ci
npm run dev
```

需要加载经典 0/1/2/3 世界线路时，在首次准备运行目录后执行 `python3 scripts/prepare-runtime.py --refresh-classic-route`，再重启 Compose 服务。该命令保留账号与角色存档，接入源数据包中 570 张可解析经典地图、经过地图筛选的原始刷怪表、商人/固定 NPC/守卫定义以及全部 142 个 `Market_Def` NPC 脚本；其中比奇省、沃玛森林、毒蛇山谷、盟重省、兽人古墓、天然洞穴、沃玛、矿区、祖玛、地牢、封魔谷、石墓、白日门、赤月峡谷、苍月岛和幻境等 121 张精选路线提供分层向导与区域刷怪，完整目录可从扩展路线向导分页进入；同时导入源 `MapQuest.txt` 的 Q001 地图任务、老人 NPC 触发脚本和 Q001–Q010 脚本依赖，服务端会在所属地图确认击杀后进入原生 Q 脚本；`--refresh-p0` 仍只生成最小 P0 世界。

打开 http://127.0.0.1:5173/ 。`npm run build` 执行 TypeScript 检查并生成 `dist/web`。下载源及 SHA-256 锁定在 `content/classic-176/asset-sources.json`；源文件变化时导入器会拒绝继续。当前图库为待原端对照的候选，不能据此认定完整 1.76 素材一致。

动作校验页：http://127.0.0.1:5173/actors.html 。可查看基础男女角色、头发、大刀卫士、鸡、鹿、多钩猫、山洞蝙蝠、羊、虎蛇、洞蛆、盔甲虫候选、沃玛战士、沃玛勇士、红野猪、黑野猪、僵尸 1/2/3、毒蜘蛛候选、骷髅、掷斧骷髅、骷髅战士、骷髅战将、祖玛系候选的八方向站立、行走与攻击。导入命令同时处理这些素材；页面只核验动作，尚未连接游戏服务器。

## 本机开发

WebSocket 网关已接入账号注册、三职业角色创建、登录、选角、入图、移动、基础近战、施法、组队、玩家交易、攻击模式、行会面板、行会战宣战、攻城申请、挖肉、NPC 对话、四条早期任务、商店买卖、仓库存取、背包、装备、药品、丢弃和拾取。先运行 `bash scripts/build-gateway.sh`，再运行 `bash scripts/compose.sh up -d web-gateway`。接口与当前限制见 [docs/web-protocol.md](docs/web-protocol.md)。联机页通过 Vite 的同源 `/ws` 代理连接网关；静态生产部署的入口代理仍需配置。

需要 Git、Python 3、Docker（Apple Silicon 可使用 Colima），以及 Docker Compose 或独立的 docker-compose。所有服务程序从固定提交源码构建。

```sh
git submodule update --init --recursive
bash scripts/build-server.sh
bash scripts/build-gateway.sh
python3 scripts/prepare-runtime.py
bash scripts/compose.sh up -d
python3 scripts/wait-ready.py
```

运行数据与本机随机数据库密码存放在忽略版本控制的 `.runtime/`。导入器排除源包中的账号和角色记录。首次初始化数据库后，再次修改 SQL 文件不会自动迁移已有数据库。

默认世界配置用于 P0 验证：比奇、兽人古墓一层、鸡、鹿，以及古墓一层的骷髅、掷斧骷髅、骷髅战士、骷髅战将和骷髅精灵。执行 `--refresh-classic-route` 后，570 张可解析地图会使用真实导出的地图块，并按源数据包筛选出 3,000 余条刷怪定义；其中 121 张精选路线由世界向导提供分层传送和区域刷怪，其他地图从完整目录进入并提供安全落点和通用返城向导。沃玛森林、毒蛇山谷、盟重省分别刷新半兽人/多钩猫、虎蛇/毒蜘蛛、羊/盔甲虫，洞穴线路刷新山洞蝙蝠、沃玛战士、沃玛勇士、僵尸、洞蛆、红野猪、黑野猪和祖玛系怪物。四类普通骨系怪物在 P0 运行目录使用 `content/classic-176/p0/skeleton-drops.txt` 的确定性金币与小量金创药掉落，用于重复验收击杀和拾取；骷髅精灵使用 `skeleton-spirit-drops.txt` 的 500 金币与中量魔法药配置。完整 1.76 配置仍保留在上游数据源，完整内容还需按版本清单筛选。修改过配置后请先备份。

## 检查

```sh
bash scripts/compose.sh ps
python3 -m unittest discover -s tests -p 'test_*.py'
python3 tools/content_audit.py --json .runtime/reports/content-audit.json --markdown .runtime/reports/content-audit.md
python3 tools/skill_visual_audit.py --json .runtime/reports/skill-visual-audit.json
python3 tools/monster_visual_audit.py --json --output .runtime/reports/monster-visual-audit.json
python3 tools/quest_catalog_audit.py --json .runtime/reports/quest-catalog-audit.json
node tools/movement_replay.mjs
node tools/web_register_probe.mjs
node tools/character_creation_probe.mjs
# 生成同一出生区的两个临时账号，再验证双客户端 PK 同步
node tools/pvp_fixture_setup.mjs
node tools/pvp_probe.mjs
node tools/trade_probe.mjs
MIR2_STABILITY_MS=30000 node tools/session_stability_probe.mjs
node tools/power_loss_probe.mjs
python3 tools/protocol_probe.py
# 固定 .NET 8 容器回归行会联盟持久化与首次宣战登记
bash scripts/run-castle-regression.sh
# 可选：移动到测试刷怪区，攻击鸡并验证挖肉入包
python3 tools/protocol_probe.py --combat
python3 tools/protocol_probe.py --drop-pickup
```

协议探针使用独立随机测试账号；凭据保存在 `.runtime/probe-account.json`。协议探针已实际通过账号注册、登录、服务器选择、角色创建/查询/选择、公告确认、比奇入图、步行、鸡的击杀/经验、挖肉入包、重新登录及服务重启后的位置与物品恢复；成功结果仅覆盖报告明确列出的项目。启动日志位于 `.runtime/logs/`，构建与素材审计报告位于 `.runtime/reports/`。

`tools/pvp_fixture_setup.mjs` 会创建两个临时账号和战士角色，并从多个出生点候选中选出同一可见区域的组合，写入 `.runtime/pvp-attacker.json` 与 `.runtime/pvp-victim.json`；创建角色请求包含旧选角服务的节流等待。随后 `tools/pvp_probe.mjs` 验证两个独立 WebSocket 会话的同图发现、相邻攻击、攻击模式和双方 HP 事件。重复运行前重新生成这两个临时凭据，避免上一轮攻击造成角色死亡。

`tools/session_stability_probe.mjs` 在指定时长内重复请求背包和攻击模式快照，检查 WebSocket 序列号与地图世代单调递增，然后主动断线并以同一角色重连；默认运行 30 秒，可通过 `MIR2_STABILITY_MS` 设置 5 秒到 1 小时的窗口。结果写入 `.runtime/reports/session-stability.json`。当前已用 5 分钟窗口完成 100 次周期快照，重连前后均收到 `attributes`、`equipment`、`inventory`、`skills` 四组权威状态。

`tools/power_loss_probe.mjs` 会先正常停服保存探针角色，移动到相邻格后在 `SaveHumanRcdTime=60000` 窗口内强制终止引擎，再重启服务并验证恢复到基线坐标；默认故障延迟 1 秒，可用 `MIR2_POWER_LOSS_DELAY_MS` 设置 250–59999ms。结果写入 `.runtime/reports/power-loss.json`，脚本结束时会尝试恢复引擎和 Web 网关。

`tools/content_audit.py` 是只读的版本内容审计器。它会检查版本清单中的 570 张地图、源地图与 Web 分块、Tiles/SmTiles/Objects 依赖、完整目录生成结果，以及已锁定的角色、怪物、物品、特效和音频素材。`tools/monster_visual_audit.py` 另按服务端 `RaceImg:Appr` 二元外观字段核对 P0 的 22 个经典怪物名称；当前 22/22 已有可加载库，其中 4 项标记为候选外观。需要在发布前验证原始文件 SHA-256 时追加 `--verify-hashes`；报告中的“已声明待校准项”仍表示原端视觉和数值需要继续核对。

`tools/movement_replay.mjs` 在当前地图执行可回退的走/跑时序回放，记录服务端接受/阻挡、方向、WebSocket 序列号和地图世代；现在还按 600ms 走路、400ms 跑步周期连续往返并记录发送间隔；结果写入 `.runtime/reports/movement-replay.json`。

本机旧协议入口为 `127.0.0.1:17000`、`17100`、`17200`，WebSocket 入口为 `127.0.0.1:18800/ws`。启动后选角网关连接数据库可能需要约 20 秒，可在 `.runtime/logs/DBSrv.log` 确认“角色网关…已打开”再运行探针。数据库未暴露宿主端口。

```sh
bash scripts/compose.sh stop
```

正常停服会先断开 Web 网关，通过游戏服务控制台保存全部在线角色并等待数据库确认，再依次关闭游戏引擎和数据库。GameSrv 停止过程可等待且幂等，在线角色实测日志只有一次后台服务停止，未产生未处理异常。真实浏览器已连续验证四组停服前后坐标，其中最终一次为 `288,615 → 288,615`，金币 73、经验 2485 同时保持。`SaveHumanRcdTime=60000` 已通过异常终止回归：WebCheck 从 `294,610` 移到 `295,610`，等待 65 秒后强制终止引擎，重启登录仍恢复到 `295,610`。`tools/power_loss_probe.mjs` 又在 1,001ms 内强制杀掉引擎，验证探针角色 `300,621 → 300,622` 恢复到最近一次正常保存坐标。真实主机断电和更长时间稳定性仍待验收，测试世界仍不应用于重要长期存档。

## 存档备份与恢复

备份工具复用正常停服顺序，收到角色存档的数据库确认后才关闭引擎，再导出三个 MySQL 数据库及行会、沙巴克文件。数据库口令不会写入归档。操作结束后会恢复原先正在运行的服务：

```sh
python3 scripts/backup.py create
python3 scripts/backup.py verify .runtime/backups/mir2-save-YYYYMMDD-HHMMSS.tar.gz
python3 scripts/backup.py restore .runtime/backups/mir2-save-YYYYMMDD-HHMMSS.tar.gz --yes
```

归档写入数据库与文件的 SHA-256，恢复前会检查清单、路径和全部哈希。恢复会替换当前账号、角色、游戏数据、行会和城堡状态，先选择明确的归档再执行。

## 当前证据与缺口

- 固定 OpenMir2 与配套数据源码，补丁保存在 `patches/openmir2/`。
- 经典脚本兼容层已补齐常见旧命令别名，修复刷怪线程诊断日志对空掉落列表的读取；引擎重建后未再出现 `WorldEngine::RegenMonsters` 异常，长期稳定性仍继续观察。
- 六个服务已在 Linux ARM64 容器启动；MySQL 独立保存账号、角色与游戏数据。
- 已审计 570 张地图，全部符合当前经典格式解析器；其中 4 张保留历史尾数据，解析器记录尾部字节并按服务端边界导出。
- 比奇地图可确定性导出为地图块；地图测试覆盖坐标、碰撞、截断检测和块重组。
- 地面丢弃/拾取已通过真实协议验证，覆盖物品身份、肉类持久扣减及地面对象移除。
- Items 与 DnItems 候选库已锁定哈希并分别导出背包 5,206 帧和地面 5,128 帧；浏览器真实显示物品图标、名称和持久。
- P0 七把武器所需的 `CWeapon01/02/03/04/06/19` 已锁定哈希并导出 4,992 个完整动作帧。浏览器按服务端外观字段选择武器库，男女分别使用 0 与 416 帧偏移，并按八方向把武器画在身体前方或后方；真实战士已完成木剑穿戴、卸下、再次穿戴和向右移动画面核验。
- `NPC00` 的 42 个静态帧已锁定哈希并全部导出。浏览器按服务端外观编号显示商人和功能 NPC；真实战士在 283,616 的场景同时看到十名具名 NPC 的原始像素图，圆形占位在素材加载后消失。
- `Magic` 与 `Magic2` 的首批 208 个原始特效帧已锁定哈希并按需导出。浏览器以服务端 17、638 消息驱动火球施法与 16 方飞行/命中、治愈施法与落点、雷电蓄力与落雷；逐帧实查显示火球追踪移动中的鸡并在目标处爆开，绿色治愈粒子包围 `Tao905`，完整雷柱落在服务端选定怪物坐标。
- WebSocket 探针与真实浏览器都已完成服务端权威的丢弃—移动—拾取闭环；探针同时校验角色能力包中的 HP、MP 与负重。
- 联机页提供实时地面物品列表。点击远处掉落物会按服务端确认连续接近并自动拾取；WebCheck 实测蜡烛从 `(290,619)` 掉落后，角色从 `(289,621)` 自动回到 `(290,619)`，条目随服务端移除并恢复到背包。
- 真实浏览器已把 5 HP 的鸡逐次打到 0 HP，收到 5 点经验，再从尸体挖出鸡肉并实时加入背包。
- 点击活体怪物会持续攻击；D001 骷髅实测 HP 从 `90 → 87 → 82 → 75`，受击事件和攻击动作连续到达，手动移动后攻击状态解除。
- 真实浏览器已由服务端确认木剑与布衣穿戴、重登装备恢复、两件装备卸回背包，以及小量金创药成功消耗。
- 角色面板已显示服务端能力数据；穿上木剑和布衣后，实测攻击从 1–1 变为 3–6，防御从 0–0 变为 0–2，并同步背包、穿戴与腕力负重。
- 浏览器从空账号完成注册，创建女性法师并进入比奇；初始四件物品正确，金创药把 HP 从 15/16 回复到 16/16，重登后角色等级和药品消耗状态保持。
- 方向键/WASD 与 Shift 跑步已接入；浏览器实测右移一格 289,616 → 290,616，再跑两格到 292,616，均由服务端确认，角色在格间逐帧移动。
- 点击附近或远处的目标会按服务端确认逐格自动接近；WebCheck 实测从 291,616 走到 285,610，与 7 格外的边界导师进入一格后自动交谈。遇到阻挡会停止并保留服务端原因。
- 浏览器已在 291,614 与边界老兵交谈，显示真实服务端脚本文本，并选择奖励入口进入包含四个选项的二级菜单。
- 浏览器已读取边界杂货店的 8 种实时商品和 10 个蜡烛实例；金币不足购买得到服务端原因码。随后木剑经询价以 25 金币卖出，并以 11 金币购入一个指定蜡烛实例，库存 20 → 19、金币 0 → 25 → 14，重登后金币 14、两支蜡烛及木剑移除状态保持。
- 商人修理已接入。浏览器在边界武器店读取服务端 668 修理清单，对满持久蜡烛显示“无需或无法修理”；战士木剑经战斗磨损后，数据库中的同一实例从 3970/4000 经 0 金币普通修理恢复到 4000/4000。网关也处理了成功对话先于 669 结果到达的消息重排。
- 装备窗实时处理服务端 642 持久变化。战士木剑在保持穿戴的战斗过程中由 4000/4000 更新到 3976/4000，持久归零也会从装备槽移除。
- `version-profile.json` 已固定 P0 的 36 件基础物品清单和 15 个核心技能。`content/classic-176/skill-rules.json` 进一步锁定每个技能的效果号、魔耗、伤害/治疗参数、职业、三级门槛、修炼门槛和延迟；`tools/skill_catalog_audit.py` 会逐字段对照运行 SQL，当前 15/15、缺失 0、重复 ID 0、规则差异 0。`apps/web/src/magic-effects.ts` 为这 15 个技能提供显式帧序或被动标记，避免被动剑术误用通用魔法动画。完整技能实战数值、施法限制和视觉场景仍按 P4 推进。个人游玩档示例位于 `content/classic-176/personal-profile.example.json`，`scripts/apply-personal-profile.py` 默认只显示变更计划，追加 `--apply` 后可设置经验倍率、掉落概率、刷怪间隔和指定角色的本机 GM 条目，重启引擎后生效。边界测试区增加武器、服装、药品、戒指、手镯、项链六个分类商店，真实浏览器在服务重启后立即读取到 7、8、4、4、4、5 种商品；连同已验证的 8 种杂货，覆盖清单全部物品。修复了新商人启动五分钟后才补货且每轮只补一类商品的问题，补丁为 `0004-stock-all-merchant-goods-on-startup.patch`。
- P0 地图加入复用经典麦家铺子脚本的边界仓库，生成逻辑位于 `scripts/prepare-runtime.py`。浏览器把蜡烛实例 10592383 存入并取回，随后再次存入；重新登录后的仓库目录仍返回同一实例号与 8.0/8.0 持久，最终取回后背包恢复为两支蜡烛。
- 修复旧引擎脚本动作编号整体偏移一位的问题；P0 技能导师在边界村和银杏村两处出生区提供同一套三职业测试。女性法师的火球术把鸡从 5 HP 打到 1 HP，熟练度 0 → 7；道士自施治愈术后收到服务端 `2/2` 效果，HP 21 → 29，熟练度 0 → 3；战士学习基本剑术并装备木剑后击杀 5 HP 的鸡，经验 0 → 5，技能熟练度 0 → 5。三个角色重登后等级、技能与熟练度均由服务端恢复，战士装备和经验也保持。
- 浏览器收到角色死亡事件后显示 0/30 HP 和回城面板；回城按钮使用内存中的登录凭据自动建立新会话。实测角色从 285,608 返回安全区 287,616，以 14/30 HP 复活，等级、金币、布衣、两支蜡烛、火球术及 7 点熟练度完整保留。
- 浏览器可在同一会话从比奇 285,608 传送到兽人古墓一层 152,362，再由古墓向导返回；换图后角色、背包、装备、属性和技能状态保留。D001 的 400×400 地图导出为 49 个分块，实测场景未解析引用为零。
- 经典主世界线路已落地：地图 0（比奇省，700×700）、1（沃玛森林，600×600）、2（毒蛇山谷，600×600）、3（盟重省，1000×800）均由原始 `.map` 导出为浏览器分块，`--refresh-classic-route` 生成向导脚本、出生点与区域刷怪。真实浏览器从地图 0 传送并重连到地图 1（240,300）、地图 2（510,474）和地图 3（332,328），三处状态栏分别显示 390、565、354 个当前视口图块且未解析引用为零；地图 3 已观察到盔甲虫与羊。
- 经典洞穴与扩展线路共 121 张精选地图，均由原始 `.map` 导出，浏览器可由扩展路线向导进入并通过层间向导返回；真实浏览器已完成 D501→D502 同会话入图回归，以及白日门 `(250,250)` 进入回归，地图视口未解析引用为零。完整目录另外收录源数据包中的其余 449 张可解析地图，每张都有原始分块、碰撞数据、安全出生点和通用返城向导；EM100、T118、T218、T318 的历史尾数据由解析器记录并按服务端读取边界导出。
- 兽人古墓一层入口保持安全，向内走到 162,354 后可看到服务端生成的骷髅原始动画；骷髅会追击和造成权威伤害，实测把测试角色从 30 HP 打到 0 并触发死亡面板。洞蛆、沃玛系、猪系和僵尸系新增库已接入服务端外观映射，动作页八方向加载无缺失；新增怪物尚需原端逐帧对照。
- 修复脚本执行器在第一条已注册动作后提前结束的问题；成功动作现在继续执行，动作失败仍会终止。导师脚本可在一次选择中完成升级、补充状态、教授三级雷电术并发放药品。真实浏览器用三次雷电术把 90 HP 骷髅打到 0，经验 0 → 85；随后拾取金币和小量金创药，金币 14 → 73，药品即时进入背包。
- D001 已生成 500 HP、600 经验、300 秒刷新的骷髅精灵首领。隔离的 P0 首领入口会清除旧测试怪、生成一只首领、补满 HP/MP 并临时开启无敌，首领测试官返回比奇时解除。真实浏览器以三级雷电术完成 `500 → 0 HP` 击杀，经验 `2485 → 3085`；中量魔法药进入背包，金币 `73 → 751`，两项地面记录都已移除。500 金币参数会经过旧引擎随机散落公式，本次实际为 678。首领当前把服务端外观 150 映射到 `Monster026` 候选素材，仍需用原端素材校准。
- 修复无效施法目标关闭整条 WebSocket 会话的问题；可预期的用户命令错误现在返回 `command_rejected` 并继续会话，浏览器同时解除技能等待态。真实浏览器对死亡首领再次施法收到 `Magic target is unavailable`，随后仍可从 `(194,335)` 移动到 `(194,334)`；丢下再拾回中量魔法药的回归数量为 `23 → 22 → 23`。
- 浏览器聊天已走真实旧协议链路。发送端支持附近、喊话、私聊、组队和行会频道，私聊会要求角色名；WebCheck 发送“聊天频道回归”后收到 `WebCheck:聊天频道回归`，低等级喊话由服务端返回等级限制且连接保持。网关限制消息为 180 个 GBK 字节并拒绝控制字符、非法频道和非法私聊对象。
- 浏览器组队面板已走真实旧协议链路。允许组队、创建、邀请和移除分别映射到 1019–1022，服务端 659、660–667 的结果和成员列表会投影到两个会话；真实双浏览器已在比奇 `(289,618)` 验证 `WebCheck` 与 `Group905` 组成两人队伍，临时 `Group905` 账号已删除。
- 浏览器玩家交易面板已走真实旧协议链路。网关把发起、放入/取回物品、设置金币、确认和取消映射到 1025–1030；历史双浏览器已验证金币和蜡烛交换，当前 `tools/trade_probe.mjs` 又用两个独立 WebSocket 会话完成同图相邻交易，确认 `tradeOpened`、物品放入、双方 `tradeSuccess` 和接收方 `itemAdded`，报告写入 `.runtime/reports/trade.json`。
- 浏览器攻击模式面板已走真实旧协议链路。七档选择映射到 1046，服务端通过 213 回传模式值；`WebCheck` 实测“全体攻击 → 红名攻击 → 全体攻击”，页面下拉框、服务端状态和系统消息保持一致。
- 浏览器行会面板已接入旧协议的打开行会、成员列表、国王 NPC 创建行会、邀请和移除、公告、封号配置、结盟和解除联盟、宣战与攻城申请入口；附近其他玩家现在可作为 PK/行会战目标，攻击模式、和平规则和行会关系仍由服务端裁决。创建、宣战和攻城请求都带当前国王 NPC 绑定进入 1011，治理操作映射 1035–1045，并投影 750、753、754、756、757–763、768–771。战争关系中的 `+目标行会 剩余毫秒` 会投影为 `warGuildTimers`，浏览器面板按秒显示倒计时，服务端发送行会名与剩余时间以保持旧协议数据稳定。封号配置使用 `编号|封号|成员1,成员2` 行格式，服务端重新校验掌门权限、成员在线状态、目标行会、宣战金币、祖玛头像和城堡日期。联盟关系现在写入行会文件并在重启后恢复，首次宣战会创建双向战争记录；`tests/CastleRegression` 已覆盖联盟持久化、对象字符串污染、首次宣战注册、重复宣战续期、占领保护时间、敌方存活阻挡、皇宫换主和结束战役清场。攻城 NPC 菜单逐项显示城堡名称，非法城堡编号、空城门状态和守卫雇佣下标已做服务端边界校验。空行会运行库的 `WebCheck` 实测收到 754；补充金币与沃玛号角后，通过比奇国王 NPC 创建 `WebGuild`，页面显示掌门人与成员列表，服务端扣除创建费用并移除号角。`tools/pvp_probe.mjs` 已用两个独立 WebSocket 会话验证同图玩家发现、相邻 PK、攻击模式和双方 HP 事件。完整沙巴克联机场景和联盟战斗仍待专项数据回归。
- 比奇试炼已接入比奇老兵 NPC。原生脚本使用角色 `QuestFlag` 的 1001/1002 旗标保存接受与完成状态，`0014-persist-quest-flags.patch` 将三组 128 字节任务数组写入 MySQL `characters_quest` 并在登录时恢复；浏览器从 NPC 对话中的任务标记更新任务日志。真实浏览器完成“接受试炼 → 收集鸡肉 → 交付并领取 1000 经验与 3 个小量金创药”，服务重启后仍显示已完成。
- 猎人试炼已接入边界猎人 NPC。原生脚本使用 1003/1004 旗标，鹿的 `MonItems` 掉落配置与服务端兜底剥取逻辑保证尸体可产出标准“肉”；真实浏览器完成“击杀鹿 → 连续挖肉 → 交付肉 → 领取 600 经验与 2 个小量金创药”，并验证任务完成、背包消耗与重登恢复。`NpcProjection` 将多个 `QMARK` 标记投影为 `quests` 数组，同时保留旧版单个 `quest` 字段。
- 药剂筹备与铁匠试炼分别使用 1005/1006、1007/1008 旗标，在比奇、银杏和边界出生区提供可见 NPC；任务覆盖药品/木剑交付、`GIVE` 奖励、经验更新、放弃后重接和 QMARK 状态，内容回归见 `tests/test_p0_quests.py`。
- 动态门已接入旧协议 `CM_OPENDOOR`、`SM_OPENDOOR_OK` 和 `SM_CLOSEDOOR`。网关校验相邻坐标后转发开门请求，服务端修正门存在性判断，按门组同步连续门格的通行状态并广播门坐标，浏览器在服务端确认后绘制开闭状态并重试原先被门阻挡的一步；真实浏览器已穿过比奇省动态门组，协议回归覆盖开门和关门字段。
- 已锁定并导入 44.1 kHz、16 位 PCM 的脚步、挥击、受击、骷髅攻击和升级 WAV，共 930,940 字节。浏览器按权威事件触发相应声音，声音开关保存在本机浏览器；实查开关的开、关状态和开启后 289,618 → 290,618 的移动事件。
- 已验证 60 秒自动保存窗口覆盖一次引擎强制终止后的坐标恢复，并用 `tools/power_loss_probe.mjs` 在 1,001ms 内完成未保存移动回退；5 分钟网关稳定性窗口也已通过。仍需真实主机断电、多小时运行、图像声音逐帧校准，补齐完整装备叠层、技能与地图动态内容及交付验收。当前 `CArmour00–13` 已按服务端衣服形状进入浏览器角色渲染，十四套首区衣服库共 22,624 个非空帧并通过导出回归；P0 22 个经典怪物均已按 `RaceImg:Appr` 外观对接，审计报告写入 `.runtime/reports/monster-visual-audit.json`。
